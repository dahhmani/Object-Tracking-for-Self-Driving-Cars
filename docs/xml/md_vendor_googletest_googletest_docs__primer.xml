<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.13">
  <compounddef id="md_vendor_googletest_googletest_docs__primer" kind="page">
    <compoundname>md_vendor_googletest_googletest_docs_Primer</compoundname>
    <title>Introduction: Why Google C++ Testing Framework?</title>
    <detaileddescription>
<para><emphasis>Google C++ Testing Framework</emphasis> helps you write better C++ tests.</para><para>No matter whether you work on Linux, Windows, or a Mac, if you write C++ code, Google Test can help you.</para><para>So what makes a good test, and how does Google C++ Testing Framework fit in? We believe:<orderedlist>
<listitem><para>Tests should be <emphasis>independent</emphasis> and <emphasis>repeatable</emphasis>. It&apos;s a pain to debug a test that succeeds or fails as a result of other tests. Google C++ Testing Framework isolates the tests by running each of them on a different object. When a test fails, Google C++ Testing Framework allows you to run it in isolation for quick debugging.</para></listitem></orderedlist>
<orderedlist>
<listitem><para>Tests should be well <emphasis>organized</emphasis> and reflect the structure of the tested code. Google C++ Testing Framework groups related tests into test cases that can share data and subroutines. This common pattern is easy to recognize and makes tests easy to maintain. Such consistency is especially helpful when people switch projects and start to work on a new code base.</para></listitem></orderedlist>
<orderedlist>
<listitem><para>Tests should be <emphasis>portable</emphasis> and <emphasis>reusable</emphasis>. The open-source community has a lot of code that is platform-neutral, its tests should also be platform-neutral. Google C++ Testing Framework works on different OSes, with different compilers (gcc, MSVC, and others), with or without exceptions, so Google C++ Testing Framework tests can easily work with a variety of configurations. (Note that the current release only contains build scripts for Linux - we are actively working on scripts for other platforms.)</para></listitem></orderedlist>
<orderedlist>
<listitem><para>When tests fail, they should provide as much <emphasis>information</emphasis> about the problem as possible. Google C++ Testing Framework doesn&apos;t stop at the first test failure. Instead, it only stops the current test and continues with the next. You can also set up tests that report non-fatal failures after which the current test continues. Thus, you can detect and fix multiple bugs in a single run-edit-compile cycle.</para></listitem></orderedlist>
<orderedlist>
<listitem><para>The testing framework should liberate test writers from housekeeping chores and let them focus on the test <emphasis>content</emphasis>. Google C++ Testing Framework automatically keeps track of all tests defined, and doesn&apos;t require the user to enumerate them in order to run them.</para></listitem></orderedlist>
<orderedlist>
<listitem><para>Tests should be <emphasis>fast</emphasis>. With Google C++ Testing Framework, you can reuse shared resources across tests and pay for the set-up/tear-down only once, without making tests depend on each other.</para></listitem></orderedlist>
</para><para>Since Google C++ Testing Framework is based on the popular xUnit architecture, you&apos;ll feel right at home if you&apos;ve used JUnit or PyUnit before. If not, it will take you about 10 minutes to learn the basics and get started. So let&apos;s go!</para><para><emphasis>Note:</emphasis> We sometimes refer to Google C++ Testing Framework informally as <emphasis>Google Test</emphasis>.</para><para><heading level="1">Setting up a New Test Project</heading>
</para><para>To write a test program using Google Test, you need to compile Google Test into a library and link your test with it. We provide build files for some popular build systems: <computeroutput>msvc/</computeroutput> for Visual Studio, <computeroutput>xcode/</computeroutput> for Mac Xcode, <computeroutput>make/</computeroutput> for GNU make, <computeroutput>codegear/</computeroutput> for Borland C++ Builder, and the autotools script (deprecated) and <computeroutput>CMakeLists.txt</computeroutput> for CMake (recommended) in the Google Test root directory. If your build system is not on this list, you can take a look at <computeroutput>make/Makefile</computeroutput> to learn how Google Test should be compiled (basically you want to compile <computeroutput>src/gtest-all.cc</computeroutput> with <computeroutput>GTEST_ROOT</computeroutput> and <computeroutput>GTEST_ROOT/include</computeroutput> in the header search path, where <computeroutput>GTEST_ROOT</computeroutput> is the Google Test root directory).</para><para>Once you are able to compile the Google Test library, you should create a project or build target for your test program. Make sure you have <computeroutput>GTEST_ROOT/include</computeroutput> in the header search path so that the compiler can find <computeroutput>&quot;gtest/gtest.h&quot;</computeroutput> when compiling your test. Set up your test project to link with the Google Test library (for example, in Visual Studio, this is done by adding a dependency on <computeroutput>gtest.vcproj</computeroutput>).</para><para>If you still have questions, take a look at how Google Test&apos;s own tests are built and use them as examples.</para><para><heading level="1">Basic Concepts</heading>
</para><para>When using Google Test, you start by writing <emphasis>assertions</emphasis>, which are statements that check whether a condition is true. An assertion&apos;s result can be <emphasis>success</emphasis>, <emphasis>nonfatal failure</emphasis>, or <emphasis>fatal failure</emphasis>. If a fatal failure occurs, it aborts the current function; otherwise the program continues normally.</para><para><emphasis>Tests</emphasis> use assertions to verify the tested code&apos;s behavior. If a test crashes or has a failed assertion, then it <emphasis>fails</emphasis>; otherwise it <emphasis>succeeds</emphasis>.</para><para>A <emphasis>test case</emphasis> contains one or many tests. You should group your tests into test cases that reflect the structure of the tested code. When multiple tests in a test case need to share common objects and subroutines, you can put them into a <emphasis>test fixture</emphasis> class.</para><para>A <emphasis>test program</emphasis> can contain multiple test cases.</para><para>We&apos;ll now explain how to write a test program, starting at the individual assertion level and building up to tests and test cases.</para><para><heading level="1">Assertions</heading>
</para><para>Google Test assertions are macros that resemble function calls. You test a class or function by making assertions about its behavior. When an assertion fails, Google Test prints the assertion&apos;s source file and line number location, along with a failure message. You may also supply a custom failure message which will be appended to Google Test&apos;s message.</para><para>The assertions come in pairs that test the same thing but have different effects on the current function. <computeroutput>ASSERT_*</computeroutput> versions generate fatal failures when they fail, and <bold>abort the current function</bold>. <computeroutput>EXPECT_*</computeroutput> versions generate nonfatal failures, which don&apos;t abort the current function. Usually <computeroutput>EXPECT_*</computeroutput> are preferred, as they allow more than one failures to be reported in a test. However, you should use <computeroutput>ASSERT_*</computeroutput> if it doesn&apos;t make sense to continue when the assertion in question fails.</para><para>Since a failed <computeroutput>ASSERT_*</computeroutput> returns from the current function immediately, possibly skipping clean-up code that comes after it, it may cause a space leak. Depending on the nature of the leak, it may or may not be worth fixing - so keep this in mind if you get a heap checker error in addition to assertion errors.</para><para>To provide a custom failure message, simply stream it into the macro using the <computeroutput>&lt;&lt;</computeroutput> operator, or a sequence of such operators. An example: <programlisting><codeline><highlight class="normal">ASSERT_EQ(x.size(),<sp/>y.size())<sp/>&lt;&lt;<sp/>&quot;Vectors<sp/>x<sp/>and<sp/>y<sp/>are<sp/>of<sp/>unequal<sp/>length&quot;;</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">for<sp/>(int<sp/>i<sp/>=<sp/>0;<sp/>i<sp/>&lt;<sp/>x.size();<sp/>++i)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>EXPECT_EQ(x[i],<sp/>y[i])<sp/>&lt;&lt;<sp/>&quot;Vectors<sp/>x<sp/>and<sp/>y<sp/>differ<sp/>at<sp/>index<sp/>&quot;<sp/>&lt;&lt;<sp/>i;</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
</programlisting></para><para>Anything that can be streamed to an <computeroutput>ostream</computeroutput> can be streamed to an assertion macro<ndash/>in particular, C strings and <computeroutput>string</computeroutput> objects. If a wide string (<computeroutput>wchar_t*</computeroutput>, <computeroutput>TCHAR*</computeroutput> in <computeroutput>UNICODE</computeroutput> mode on Windows, or <computeroutput>std::wstring</computeroutput>) is streamed to an assertion, it will be translated to UTF-8 when printed.</para><para><heading level="2">Basic Assertions</heading>
</para><para>These assertions do basic true/false condition testing.</para><para><table rows="3" cols="3"><row>
<entry thead="yes"><para><bold>Fatal assertion</bold> </para></entry><entry thead="yes"><para><bold>Nonfatal assertion</bold> </para></entry><entry thead="yes"><para><bold>Verifies</bold>  </para></entry></row>
<row>
<entry thead="no"><para><computeroutput>ASSERT_TRUE(</computeroutput>_condition_<computeroutput>)</computeroutput>; </para></entry><entry thead="no"><para><computeroutput>EXPECT_TRUE(</computeroutput>_condition_<computeroutput>)</computeroutput>; </para></entry><entry thead="no"><para><emphasis>condition</emphasis> is true </para></entry></row>
<row>
<entry thead="no"><para><computeroutput>ASSERT_FALSE(</computeroutput>_condition_<computeroutput>)</computeroutput>; </para></entry><entry thead="no"><para><computeroutput>EXPECT_FALSE(</computeroutput>_condition_<computeroutput>)</computeroutput>; </para></entry><entry thead="no"><para><emphasis>condition</emphasis> is false </para></entry></row>
</table>
Remember, when they fail, <computeroutput>ASSERT_*</computeroutput> yields a fatal failure and returns from the current function, while <computeroutput>EXPECT_*</computeroutput> yields a nonfatal failure, allowing the function to continue running. In either case, an assertion failure means its containing test fails.</para><para><emphasis>Availability</emphasis>: Linux, Windows, Mac.</para><para><heading level="2">Binary Comparison</heading>
</para><para>This section describes assertions that compare two values.</para><para><table rows="7" cols="3"><row>
<entry thead="yes"><para><bold>Fatal assertion</bold> </para></entry><entry thead="yes"><para><bold>Nonfatal assertion</bold> </para></entry><entry thead="yes"><para><bold>Verifies</bold>  </para></entry></row>
<row>
<entry thead="no"><para><computeroutput>ASSERT_EQ(</computeroutput>_val1_<computeroutput>,</computeroutput>_val2_<computeroutput>);</computeroutput></para></entry><entry thead="no"><para><computeroutput>EXPECT_EQ(</computeroutput>_val1_<computeroutput>,</computeroutput>_val2_<computeroutput>);</computeroutput></para></entry><entry thead="no"><para><emphasis>val1</emphasis> <computeroutput>==</computeroutput> <emphasis>val2</emphasis> </para></entry></row>
<row>
<entry thead="no"><para><computeroutput>ASSERT_NE(</computeroutput>_val1_<computeroutput>,</computeroutput>_val2_<computeroutput>);</computeroutput></para></entry><entry thead="no"><para><computeroutput>EXPECT_NE(</computeroutput>_val1_<computeroutput>,</computeroutput>_val2_<computeroutput>);</computeroutput></para></entry><entry thead="no"><para><emphasis>val1</emphasis> <computeroutput>!=</computeroutput> <emphasis>val2</emphasis> </para></entry></row>
<row>
<entry thead="no"><para><computeroutput>ASSERT_LT(</computeroutput>_val1_<computeroutput>,</computeroutput>_val2_<computeroutput>);</computeroutput></para></entry><entry thead="no"><para><computeroutput>EXPECT_LT(</computeroutput>_val1_<computeroutput>,</computeroutput>_val2_<computeroutput>);</computeroutput></para></entry><entry thead="no"><para><emphasis>val1</emphasis> <computeroutput>&lt;</computeroutput> <emphasis>val2</emphasis> </para></entry></row>
<row>
<entry thead="no"><para><computeroutput>ASSERT_LE(</computeroutput>_val1_<computeroutput>,</computeroutput>_val2_<computeroutput>);</computeroutput></para></entry><entry thead="no"><para><computeroutput>EXPECT_LE(</computeroutput>_val1_<computeroutput>,</computeroutput>_val2_<computeroutput>);</computeroutput></para></entry><entry thead="no"><para><emphasis>val1</emphasis> <computeroutput>&lt;=</computeroutput> <emphasis>val2</emphasis> </para></entry></row>
<row>
<entry thead="no"><para><computeroutput>ASSERT_GT(</computeroutput>_val1_<computeroutput>,</computeroutput>_val2_<computeroutput>);</computeroutput></para></entry><entry thead="no"><para><computeroutput>EXPECT_GT(</computeroutput>_val1_<computeroutput>,</computeroutput>_val2_<computeroutput>);</computeroutput></para></entry><entry thead="no"><para><emphasis>val1</emphasis> <computeroutput>&gt;</computeroutput> <emphasis>val2</emphasis> </para></entry></row>
<row>
<entry thead="no"><para><computeroutput>ASSERT_GE(</computeroutput>_val1_<computeroutput>,</computeroutput>_val2_<computeroutput>);</computeroutput></para></entry><entry thead="no"><para><computeroutput>EXPECT_GE(</computeroutput>_val1_<computeroutput>,</computeroutput>_val2_<computeroutput>);</computeroutput></para></entry><entry thead="no"><para><emphasis>val1</emphasis> <computeroutput>&gt;=</computeroutput> <emphasis>val2</emphasis> </para></entry></row>
</table>
In the event of a failure, Google Test prints both <emphasis>val1</emphasis> and <emphasis>val2</emphasis>.</para><para>Value arguments must be comparable by the assertion&apos;s comparison operator or you&apos;ll get a compiler error. We used to require the arguments to support the <computeroutput>&lt;&lt;</computeroutput> operator for streaming to an <computeroutput>ostream</computeroutput>, but it&apos;s no longer necessary since v1.6.0 (if <computeroutput>&lt;&lt;</computeroutput> is supported, it will be called to print the arguments when the assertion fails; otherwise Google Test will attempt to print them in the best way it can. For more details and how to customize the printing of the arguments, see this Google <ref refid="class_mock" kindref="compound">Mock</ref> <ulink url="../../googlemock/docs/CookBook.md#teaching-google-mock-how-to-print-your-values">recipe</ulink>.).</para><para>These assertions can work with a user-defined type, but only if you define the corresponding comparison operator (e.g. <computeroutput>==</computeroutput>, <computeroutput>&lt;</computeroutput>, etc). If the corresponding operator is defined, prefer using the <computeroutput>ASSERT_*()</computeroutput> macros because they will print out not only the result of the comparison, but the two operands as well.</para><para>Arguments are always evaluated exactly once. Therefore, it&apos;s OK for the arguments to have side effects. However, as with any ordinary C/C++ function, the arguments&apos; evaluation order is undefined (i.e. the compiler is free to choose any order) and your code should not depend on any particular argument evaluation order.</para><para><computeroutput>ASSERT_EQ()</computeroutput> does pointer equality on pointers. If used on two C strings, it tests if they are in the same memory location, not if they have the same value. Therefore, if you want to compare C strings (e.g. <computeroutput>const char*</computeroutput>) by value, use <computeroutput>ASSERT_STREQ()</computeroutput> , which will be described later on. In particular, to assert that a C string is <computeroutput>NULL</computeroutput>, use <computeroutput>ASSERT_STREQ(NULL, c_string)</computeroutput> . However, to compare two <computeroutput>string</computeroutput> objects, you should use <computeroutput>ASSERT_EQ</computeroutput>.</para><para>Macros in this section work with both narrow and wide string objects (<computeroutput>string</computeroutput> and <computeroutput>wstring</computeroutput>).</para><para><emphasis>Availability</emphasis>: Linux, Windows, Mac.</para><para><emphasis>Historical note</emphasis>: Before February 2016 <computeroutput>*_EQ</computeroutput> had a convention of calling it as <computeroutput>ASSERT_EQ(expected, actual)</computeroutput>, so lots of existing code uses this order. Now <computeroutput>*_EQ</computeroutput> treats both parameters in the same way.</para><para><heading level="2">String Comparison</heading>
</para><para>The assertions in this group compare two <bold>C strings</bold>. If you want to compare two <computeroutput>string</computeroutput> objects, use <computeroutput>EXPECT_EQ</computeroutput>, <computeroutput>EXPECT_NE</computeroutput>, and etc instead.</para><para><table rows="5" cols="3"><row>
<entry thead="yes"><para><bold>Fatal assertion</bold> </para></entry><entry thead="yes"><para><bold>Nonfatal assertion</bold> </para></entry><entry thead="yes"><para><bold>Verifies</bold>  </para></entry></row>
<row>
<entry thead="no"><para><computeroutput>ASSERT_STREQ(</computeroutput>_str1_<computeroutput>,</computeroutput>_str2_<computeroutput>);</computeroutput> </para></entry><entry thead="no"><para><computeroutput>EXPECT_STREQ(</computeroutput>_str1_<computeroutput>,</computeroutput>_str_2<computeroutput>);</computeroutput> </para></entry><entry thead="no"><para>the two C strings have the same content </para></entry></row>
<row>
<entry thead="no"><para><computeroutput>ASSERT_STRNE(</computeroutput>_str1_<computeroutput>,</computeroutput>_str2_<computeroutput>);</computeroutput> </para></entry><entry thead="no"><para><computeroutput>EXPECT_STRNE(</computeroutput>_str1_<computeroutput>,</computeroutput>_str2_<computeroutput>);</computeroutput> </para></entry><entry thead="no"><para>the two C strings have different content </para></entry></row>
<row>
<entry thead="no"><para><computeroutput>ASSERT_STRCASEEQ(</computeroutput>_str1_<computeroutput>,</computeroutput>_str2_<computeroutput>);</computeroutput></para></entry><entry thead="no"><para><computeroutput>EXPECT_STRCASEEQ(</computeroutput>_str1_<computeroutput>,</computeroutput>_str2_<computeroutput>);</computeroutput> </para></entry><entry thead="no"><para>the two C strings have the same content, ignoring case </para></entry></row>
<row>
<entry thead="no"><para><computeroutput>ASSERT_STRCASENE(</computeroutput>_str1_<computeroutput>,</computeroutput>_str2_<computeroutput>);</computeroutput></para></entry><entry thead="no"><para><computeroutput>EXPECT_STRCASENE(</computeroutput>_str1_<computeroutput>,</computeroutput>_str2_<computeroutput>);</computeroutput> </para></entry><entry thead="no"><para>the two C strings have different content, ignoring case </para></entry></row>
</table>
Note that &quot;CASE&quot; in an assertion name means that case is ignored.</para><para><computeroutput>*STREQ*</computeroutput> and <computeroutput>*STRNE*</computeroutput> also accept wide C strings (<computeroutput>wchar_t*</computeroutput>). If a comparison of two wide strings fails, their values will be printed as UTF-8 narrow strings.</para><para>A <computeroutput>NULL</computeroutput> pointer and an empty string are considered <emphasis>different</emphasis>.</para><para><emphasis>Availability</emphasis>: Linux, Windows, Mac.</para><para>See also: For more string comparison tricks (substring, prefix, suffix, and regular expression matching, for example), see the Advanced Google Test Guide.</para><para><heading level="1">Simple Tests</heading>
</para><para>To create a test:<orderedlist>
<listitem><para>Use the <computeroutput>TEST()</computeroutput> macro to define and name a test function, These are ordinary C++ functions that don&apos;t return a value.</para></listitem></orderedlist>
<orderedlist>
<listitem><para>In this function, along with any valid C++ statements you want to include, use the various Google Test assertions to check values.</para></listitem></orderedlist>
<orderedlist>
<listitem><para>The test&apos;s result is determined by the assertions; if any assertion in the test fails (either fatally or non-fatally), or if the test crashes, the entire test fails. Otherwise, it succeeds.</para></listitem></orderedlist>
</para><para><programlisting><codeline><highlight class="normal">TEST(test_case_name,<sp/>test_name)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/>...<sp/>test<sp/>body<sp/>...</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
</programlisting></para><para><computeroutput>TEST()</computeroutput> arguments go from general to specific. The <emphasis>first</emphasis> argument is the name of the test case, and the <emphasis>second</emphasis> argument is the test&apos;s name within the test case. Both names must be valid C++ identifiers, and they should not contain underscore (<computeroutput>_</computeroutput>). A test&apos;s <emphasis>full name</emphasis> consists of its containing test case and its individual name. Tests from different test cases can have the same individual name.</para><para>For example, let&apos;s take a simple integer function: <programlisting><codeline><highlight class="normal">int<sp/>Factorial(int<sp/>n);<sp/>//<sp/>Returns<sp/>the<sp/>factorial<sp/>of<sp/>n</highlight></codeline>
</programlisting></para><para>A test case for this function might look like: <programlisting><codeline><highlight class="normal">//<sp/>Tests<sp/>factorial<sp/>of<sp/>0.</highlight></codeline>
<codeline><highlight class="normal">TEST(FactorialTest,<sp/>HandlesZeroInput)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>EXPECT_EQ(1,<sp/>Factorial(0));</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">//<sp/>Tests<sp/>factorial<sp/>of<sp/>positive<sp/>numbers.</highlight></codeline>
<codeline><highlight class="normal">TEST(FactorialTest,<sp/>HandlesPositiveInput)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>EXPECT_EQ(1,<sp/>Factorial(1));</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>EXPECT_EQ(2,<sp/>Factorial(2));</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>EXPECT_EQ(6,<sp/>Factorial(3));</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>EXPECT_EQ(40320,<sp/>Factorial(8));</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
</programlisting></para><para>Google Test groups the test results by test cases, so logically-related tests should be in the same test case; in other words, the first argument to their <computeroutput>TEST()</computeroutput> should be the same. In the above example, we have two tests, <computeroutput>HandlesZeroInput</computeroutput> and <computeroutput>HandlesPositiveInput</computeroutput>, that belong to the same test case <computeroutput>FactorialTest</computeroutput>.</para><para><emphasis>Availability</emphasis>: Linux, Windows, Mac.</para><para><heading level="1">Test Fixtures: Using the Same Data Configuration for Multiple Tests</heading>
</para><para>If you find yourself writing two or more tests that operate on similar data, you can use a <emphasis>test fixture</emphasis>. It allows you to reuse the same configuration of objects for several different tests.</para><para>To create a fixture, just:<orderedlist>
<listitem><para>Derive a class from <computeroutput><ref refid="classtesting_1_1_test" kindref="compound">testing::Test</ref></computeroutput> . Start its body with <computeroutput>protected:</computeroutput> or <computeroutput>public:</computeroutput> as we&apos;ll want to access fixture members from sub-classes.</para></listitem></orderedlist>
<orderedlist>
<listitem><para>Inside the class, declare any objects you plan to use.</para></listitem></orderedlist>
<orderedlist>
<listitem><para>If necessary, write a default constructor or <computeroutput>SetUp()</computeroutput> function to prepare the objects for each test. A common mistake is to spell <computeroutput>SetUp()</computeroutput> as <computeroutput>Setup()</computeroutput> with a small <computeroutput>u</computeroutput> - don&apos;t let that happen to you.</para></listitem></orderedlist>
<orderedlist>
<listitem><para>If necessary, write a destructor or <computeroutput>TearDown()</computeroutput> function to release any resources you allocated in <computeroutput>SetUp()</computeroutput> . To learn when you should use the constructor/destructor and when you should use <computeroutput>SetUp()/TearDown()</computeroutput>, read this <ulink url="FAQ.md#should-i-use-the-constructordestructor-of-the-test-fixture-or-the-set-uptear-down-function">FAQ entry</ulink>.</para></listitem></orderedlist>
<orderedlist>
<listitem><para>If needed, define subroutines for your tests to share.</para></listitem></orderedlist>
</para><para>When using a fixture, use <computeroutput><ref refid="test__tracker_8cpp_1a567724948cdce4788a1e8c8baf6b6cfe" kindref="member">TEST_F()</ref></computeroutput> instead of <computeroutput>TEST()</computeroutput> as it allows you to access objects and subroutines in the test fixture: <programlisting><codeline><highlight class="normal">TEST_F(test_case_name,<sp/>test_name)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/>...<sp/>test<sp/>body<sp/>...</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
</programlisting></para><para>Like <computeroutput>TEST()</computeroutput>, the first argument is the test case name, but for <computeroutput><ref refid="test__tracker_8cpp_1a567724948cdce4788a1e8c8baf6b6cfe" kindref="member">TEST_F()</ref></computeroutput> this must be the name of the test fixture class. You&apos;ve probably guessed: <computeroutput>_F</computeroutput> is for fixture.</para><para>Unfortunately, the C++ macro system does not allow us to create a single macro that can handle both types of tests. Using the wrong macro causes a compiler error.</para><para>Also, you must first define a test fixture class before using it in a <computeroutput><ref refid="test__tracker_8cpp_1a567724948cdce4788a1e8c8baf6b6cfe" kindref="member">TEST_F()</ref></computeroutput>, or you&apos;ll get the compiler error &quot;`virtual outside class
declaration`&quot;.</para><para>For each test defined with <computeroutput><ref refid="test__tracker_8cpp_1a567724948cdce4788a1e8c8baf6b6cfe" kindref="member">TEST_F()</ref></computeroutput>, Google Test will:<orderedlist>
<listitem><para>Create a <emphasis>fresh</emphasis> test fixture at runtime</para></listitem></orderedlist>
<orderedlist>
<listitem><para>Immediately initialize it via <computeroutput>SetUp()</computeroutput> ,</para></listitem></orderedlist>
<orderedlist>
<listitem><para>Run the test</para></listitem></orderedlist>
<orderedlist>
<listitem><para>Clean up by calling <computeroutput>TearDown()</computeroutput></para></listitem></orderedlist>
<orderedlist>
<listitem><para>Delete the test fixture. Note that different tests in the same test case have different test fixture objects, and Google Test always deletes a test fixture before it creates the next one. Google Test does not reuse the same test fixture for multiple tests. Any changes one test makes to the fixture do not affect other tests.</para></listitem></orderedlist>
</para><para>As an example, let&apos;s write tests for a FIFO queue class named <computeroutput><ref refid="class_queue" kindref="compound">Queue</ref></computeroutput>, which has the following interface: <programlisting><codeline><highlight class="normal">template<sp/>&lt;typename<sp/>E&gt;<sp/>//<sp/>E<sp/>is<sp/>the<sp/>element<sp/>type.</highlight></codeline>
<codeline><highlight class="normal">class<sp/>Queue<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/>public:</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>Queue();</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>void<sp/>Enqueue(const<sp/>E&amp;<sp/>element);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>E*<sp/>Dequeue();<sp/>//<sp/>Returns<sp/>NULL<sp/>if<sp/>the<sp/>queue<sp/>is<sp/>empty.</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>size_t<sp/>size()<sp/>const;</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>...</highlight></codeline>
<codeline><highlight class="normal">};</highlight></codeline>
</programlisting></para><para>First, define a fixture class. By convention, you should give it the name <computeroutput><ref refid="class_foo_test" kindref="compound">FooTest</ref></computeroutput> where <computeroutput>Foo</computeroutput> is the class being tested. <programlisting><codeline><highlight class="normal">class<sp/>QueueTest<sp/>:<sp/>public<sp/>::testing::Test<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/>protected:</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>virtual<sp/>void<sp/>SetUp()<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>q1_.Enqueue(1);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>q2_.Enqueue(2);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>q2_.Enqueue(3);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>}</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal"><sp/><sp/>//<sp/>virtual<sp/>void<sp/>TearDown()<sp/>{}</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal"><sp/><sp/>Queue&lt;int&gt;<sp/>q0_;</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>Queue&lt;int&gt;<sp/>q1_;</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>Queue&lt;int&gt;<sp/>q2_;</highlight></codeline>
<codeline><highlight class="normal">};</highlight></codeline>
</programlisting></para><para>In this case, <computeroutput>TearDown()</computeroutput> is not needed since we don&apos;t have to clean up after each test, other than what&apos;s already done by the destructor.</para><para>Now we&apos;ll write tests using <computeroutput><ref refid="test__tracker_8cpp_1a567724948cdce4788a1e8c8baf6b6cfe" kindref="member">TEST_F()</ref></computeroutput> and this fixture. <programlisting><codeline><highlight class="normal">TEST_F(QueueTest,<sp/>IsEmptyInitially)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>EXPECT_EQ(0,<sp/>q0_.size());</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">TEST_F(QueueTest,<sp/>DequeueWorks)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>int*<sp/>n<sp/>=<sp/>q0_.Dequeue();</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>EXPECT_EQ(NULL,<sp/>n);</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal"><sp/><sp/>n<sp/>=<sp/>q1_.Dequeue();</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>ASSERT_TRUE(n<sp/>!=<sp/>NULL);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>EXPECT_EQ(1,<sp/>*n);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>EXPECT_EQ(0,<sp/>q1_.size());</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>delete<sp/>n;</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal"><sp/><sp/>n<sp/>=<sp/>q2_.Dequeue();</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>ASSERT_TRUE(n<sp/>!=<sp/>NULL);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>EXPECT_EQ(2,<sp/>*n);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>EXPECT_EQ(1,<sp/>q2_.size());</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>delete<sp/>n;</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
</programlisting></para><para>The above uses both <computeroutput>ASSERT_*</computeroutput> and <computeroutput>EXPECT_*</computeroutput> assertions. The rule of thumb is to use <computeroutput>EXPECT_*</computeroutput> when you want the test to continue to reveal more errors after the assertion failure, and use <computeroutput>ASSERT_*</computeroutput> when continuing after failure doesn&apos;t make sense. For example, the second assertion in the <computeroutput>Dequeue</computeroutput> test is <computeroutput>ASSERT_TRUE(n != NULL)</computeroutput>, as we need to dereference the pointer <computeroutput>n</computeroutput> later, which would lead to a segfault when <computeroutput>n</computeroutput> is <computeroutput>NULL</computeroutput>.</para><para>When these tests run, the following happens:<orderedlist>
<listitem><para>Google Test constructs a <computeroutput><ref refid="class_queue_test" kindref="compound">QueueTest</ref></computeroutput> object (let&apos;s call it <computeroutput>t1</computeroutput> ).</para></listitem></orderedlist>
<orderedlist>
<listitem><para><computeroutput>t1.SetUp()</computeroutput> initializes <computeroutput>t1</computeroutput> .</para></listitem></orderedlist>
<orderedlist>
<listitem><para>The first test ( <computeroutput>IsEmptyInitially</computeroutput> ) runs on <computeroutput>t1</computeroutput> .</para></listitem></orderedlist>
<orderedlist>
<listitem><para><computeroutput>t1.TearDown()</computeroutput> cleans up after the test finishes.</para></listitem></orderedlist>
<orderedlist>
<listitem><para><computeroutput>t1</computeroutput> is destructed.</para></listitem></orderedlist>
<orderedlist>
<listitem><para>The above steps are repeated on another <computeroutput><ref refid="class_queue_test" kindref="compound">QueueTest</ref></computeroutput> object, this time running the <computeroutput>DequeueWorks</computeroutput> test.</para></listitem></orderedlist>
</para><para><emphasis>Availability</emphasis>: Linux, Windows, Mac.</para><para><emphasis>Note</emphasis>: Google Test automatically saves all <emphasis>Google Test</emphasis> flags when a test object is constructed, and restores them when it is destructed.</para><para><heading level="1">Invoking the Tests</heading>
</para><para><computeroutput>TEST()</computeroutput> and <computeroutput><ref refid="test__tracker_8cpp_1a567724948cdce4788a1e8c8baf6b6cfe" kindref="member">TEST_F()</ref></computeroutput> implicitly register their tests with Google Test. So, unlike with many other C++ testing frameworks, you don&apos;t have to re-list all your defined tests in order to run them.</para><para>After defining your tests, you can run them with <computeroutput>RUN_ALL_TESTS()</computeroutput> , which returns <computeroutput>0</computeroutput> if all the tests are successful, or <computeroutput>1</computeroutput> otherwise. Note that <computeroutput>RUN_ALL_TESTS()</computeroutput> runs <emphasis>all tests</emphasis> in your link unit <ndash/> they can be from different test cases, or even different source files.</para><para>When invoked, the <computeroutput>RUN_ALL_TESTS()</computeroutput> macro:<orderedlist>
<listitem><para>Saves the state of all Google Test flags.</para></listitem></orderedlist>
<orderedlist>
<listitem><para>Creates a test fixture object for the first test.</para></listitem></orderedlist>
<orderedlist>
<listitem><para>Initializes it via <computeroutput>SetUp()</computeroutput>.</para></listitem></orderedlist>
<orderedlist>
<listitem><para>Runs the test on the fixture object.</para></listitem></orderedlist>
<orderedlist>
<listitem><para>Cleans up the fixture via <computeroutput>TearDown()</computeroutput>.</para></listitem></orderedlist>
<orderedlist>
<listitem><para>Deletes the fixture.</para></listitem></orderedlist>
<orderedlist>
<listitem><para>Restores the state of all Google Test flags.</para></listitem></orderedlist>
<orderedlist>
<listitem><para>Repeats the above steps for the next test, until all tests have run.</para></listitem></orderedlist>
</para><para>In addition, if the text fixture&apos;s constructor generates a fatal failure in step 2, there is no point for step 3 - 5 and they are thus skipped. Similarly, if step 3 generates a fatal failure, step 4 will be skipped.</para><para><emphasis>Important</emphasis>: You must not ignore the return value of <computeroutput>RUN_ALL_TESTS()</computeroutput>, or <computeroutput>gcc</computeroutput> will give you a compiler error. The rationale for this design is that the automated testing service determines whether a test has passed based on its exit code, not on its stdout/stderr output; thus your <computeroutput>main()</computeroutput> function must return the value of <computeroutput>RUN_ALL_TESTS()</computeroutput>.</para><para>Also, you should call <computeroutput>RUN_ALL_TESTS()</computeroutput> only <bold>once</bold>. Calling it more than once conflicts with some advanced Google Test features (e.g. thread-safe death tests) and thus is not supported.</para><para><emphasis>Availability</emphasis>: Linux, Windows, Mac.</para><para><heading level="1">Writing the main() Function</heading>
</para><para>You can start from this boilerplate: <programlisting><codeline><highlight class="normal">#include<sp/>&quot;this/package/foo.h&quot;</highlight></codeline>
<codeline><highlight class="normal">#include<sp/>&quot;gtest/gtest.h&quot;</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">namespace<sp/>{</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">//<sp/>The<sp/>fixture<sp/>for<sp/>testing<sp/>class<sp/>Foo.</highlight></codeline>
<codeline><highlight class="normal">class<sp/>FooTest<sp/>:<sp/>public<sp/>::testing::Test<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/>protected:</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>//<sp/>You<sp/>can<sp/>remove<sp/>any<sp/>or<sp/>all<sp/>of<sp/>the<sp/>following<sp/>functions<sp/>if<sp/>its<sp/>body</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>//<sp/>is<sp/>empty.</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal"><sp/><sp/>FooTest()<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>//<sp/>You<sp/>can<sp/>do<sp/>set-up<sp/>work<sp/>for<sp/>each<sp/>test<sp/>here.</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>}</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal"><sp/><sp/>virtual<sp/>~FooTest()<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>//<sp/>You<sp/>can<sp/>do<sp/>clean-up<sp/>work<sp/>that<sp/>doesn&apos;t<sp/>throw<sp/>exceptions<sp/>here.</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>}</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal"><sp/><sp/>//<sp/>If<sp/>the<sp/>constructor<sp/>and<sp/>destructor<sp/>are<sp/>not<sp/>enough<sp/>for<sp/>setting<sp/>up</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>//<sp/>and<sp/>cleaning<sp/>up<sp/>each<sp/>test,<sp/>you<sp/>can<sp/>define<sp/>the<sp/>following<sp/>methods:</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal"><sp/><sp/>virtual<sp/>void<sp/>SetUp()<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>//<sp/>Code<sp/>here<sp/>will<sp/>be<sp/>called<sp/>immediately<sp/>after<sp/>the<sp/>constructor<sp/>(right</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>//<sp/>before<sp/>each<sp/>test).</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>}</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal"><sp/><sp/>virtual<sp/>void<sp/>TearDown()<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>//<sp/>Code<sp/>here<sp/>will<sp/>be<sp/>called<sp/>immediately<sp/>after<sp/>each<sp/>test<sp/>(right</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>//<sp/>before<sp/>the<sp/>destructor).</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>}</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal"><sp/><sp/>//<sp/>Objects<sp/>declared<sp/>here<sp/>can<sp/>be<sp/>used<sp/>by<sp/>all<sp/>tests<sp/>in<sp/>the<sp/>test<sp/>case<sp/>for<sp/>Foo.</highlight></codeline>
<codeline><highlight class="normal">};</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">//<sp/>Tests<sp/>that<sp/>the<sp/>Foo::Bar()<sp/>method<sp/>does<sp/>Abc.</highlight></codeline>
<codeline><highlight class="normal">TEST_F(FooTest,<sp/>MethodBarDoesAbc)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>const<sp/>string<sp/>input_filepath<sp/>=<sp/>&quot;this/package/testdata/myinputfile.dat&quot;;</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>const<sp/>string<sp/>output_filepath<sp/>=<sp/>&quot;this/package/testdata/myoutputfile.dat&quot;;</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>Foo<sp/>f;</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>EXPECT_EQ(0,<sp/>f.Bar(input_filepath,<sp/>output_filepath));</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">//<sp/>Tests<sp/>that<sp/>Foo<sp/>does<sp/>Xyz.</highlight></codeline>
<codeline><highlight class="normal">TEST_F(FooTest,<sp/>DoesXyz)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>//<sp/>Exercises<sp/>the<sp/>Xyz<sp/>feature<sp/>of<sp/>Foo.</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">}<sp/><sp/>//<sp/>namespace</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">int<sp/>main(int<sp/>argc,<sp/>char<sp/>**argv)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>::testing::InitGoogleTest(&amp;argc,<sp/>argv);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>return<sp/>RUN_ALL_TESTS();</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
</programlisting></para><para>The <computeroutput>::testing::InitGoogleTest()</computeroutput> function parses the command line for Google Test flags, and removes all recognized flags. This allows the user to control a test program&apos;s behavior via various flags, which we&apos;ll cover in AdvancedGuide. You must call this function before calling <computeroutput>RUN_ALL_TESTS()</computeroutput>, or the flags won&apos;t be properly initialized.</para><para>On Windows, <computeroutput>InitGoogleTest()</computeroutput> also works with wide strings, so it can be used in programs compiled in <computeroutput>UNICODE</computeroutput> mode as well.</para><para>But maybe you think that writing all those main() functions is too much work? We agree with you completely and that&apos;s why Google Test provides a basic implementation of main(). If it fits your needs, then just link your test with gtest_main library and you are good to go.</para><para><heading level="2">Important note for Visual C++ users</heading>
</para><para>If you put your tests into a library and your <computeroutput>main()</computeroutput> function is in a different library or in your .exe file, those tests will not run. The reason is a <ulink url="https://connect.microsoft.com/feedback/viewfeedback.aspx?FeedbackID=244410&amp;siteid=210">bug</ulink> in Visual C++. When you define your tests, Google Test creates certain static objects to register them. These objects are not referenced from elsewhere but their constructors are still supposed to run. When Visual C++ linker sees that nothing in the library is referenced from other places it throws the library out. You have to reference your library with tests from your main program to keep the linker from discarding it. Here is how to do it. Somewhere in your library code declare a function: <programlisting><codeline><highlight class="normal">__declspec(dllexport)<sp/>int<sp/>PullInMyLibrary()<sp/>{<sp/>return<sp/>0;<sp/>}</highlight></codeline>
</programlisting> If you put your tests in a static library (not DLL) then <computeroutput>__declspec(dllexport)</computeroutput> is not required. Now, in your main program, write a code that invokes that function: <programlisting><codeline><highlight class="normal">int<sp/>PullInMyLibrary();</highlight></codeline>
<codeline><highlight class="normal">static<sp/>int<sp/>dummy<sp/>=<sp/>PullInMyLibrary();</highlight></codeline>
</programlisting> This will keep your tests referenced and will make them register themselves at startup.</para><para>In addition, if you define your tests in a static library, add <computeroutput>/OPT:NOREF</computeroutput> to your main program linker options. If you use MSVC++ IDE, go to your .exe project properties/Configuration Properties/Linker/Optimization and set References setting to <computeroutput>Keep Unreferenced Data (/OPT:NOREF)</computeroutput>. This will keep Visual C++ linker from discarding individual symbols generated by your tests from the final executable.</para><para>There is one more pitfall, though. If you use Google Test as a static library (that&apos;s how it is defined in gtest.vcproj) your tests must also reside in a static library. If you have to have them in a DLL, you <emphasis>must</emphasis> change Google Test to build into a DLL as well. Otherwise your tests will not run correctly or will not run at all. The general conclusion here is: make your life easier - do not write your tests in libraries!</para><para><heading level="1">Where to Go from Here</heading>
</para><para>Congratulations! You&apos;ve learned the Google Test basics. You can start writing and running Google Test tests, read some samples, or continue with AdvancedGuide, which describes many more useful Google Test features.</para><para><heading level="1">Known Limitations</heading>
</para><para>Google Test is designed to be thread-safe. The implementation is thread-safe on systems where the <computeroutput>pthreads</computeroutput> library is available. It is currently <emphasis>unsafe</emphasis> to use Google Test assertions from two threads concurrently on other systems (e.g. Windows). In most tests this is not an issue as usually the assertions are done in the main thread. If you want to help, you can volunteer to implement the necessary synchronization primitives in <computeroutput>gtest-port.h</computeroutput> for your platform. </para>    </detaileddescription>
  </compounddef>
</doxygen>
