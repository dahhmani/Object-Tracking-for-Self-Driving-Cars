<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.13">
  <compounddef id="md_vendor_googletest_googletest_docs__f_a_q" kind="page">
    <compoundname>md_vendor_googletest_googletest_docs_FAQ</compoundname>
    <title>FAQ</title>
    <detaileddescription>
<para>If you cannot find the answer to your question here, and you have read Primer and AdvancedGuide, send it to <ulink url="mailto:googletestframework@googlegroups.com">googletestframework@googlegroups.com</ulink>.</para><para><heading level="2">Why should I use Google Test instead of my favorite C++ testing framework?</heading>
</para><para>First, let us say clearly that we don&apos;t want to get into the debate of which C++ testing framework is <bold>the best</bold>. There exist many fine frameworks for writing C++ tests, and we have tremendous respect for the developers and users of them. We don&apos;t think there is (or will be) a single best framework - you have to pick the right tool for the particular task you are tackling.</para><para>We created Google Test because we couldn&apos;t find the right combination of features and conveniences in an existing framework to satisfy <emphasis>our</emphasis> needs. The following is a list of things that <emphasis>we</emphasis> like about Google Test. We don&apos;t claim them to be unique to Google Test - rather, the combination of them makes Google Test the choice for us. We hope this list can help you decide whether it is for you too.</para><para><itemizedlist>
<listitem><para>Google Test is designed to be portable: it doesn&apos;t require exceptions or RTTI; it works around various bugs in various compilers and environments; etc. As a result, it works on Linux, Mac OS X, Windows and several embedded operating systems.</para></listitem><listitem><para>Nonfatal assertions (<computeroutput>EXPECT_*</computeroutput>) have proven to be great time savers, as they allow a test to report multiple failures in a single edit-compile-test cycle.</para></listitem><listitem><para>It&apos;s easy to write assertions that generate informative messages: you just use the stream syntax to append any additional information, e.g. <computeroutput>ASSERT_EQ(5, Foo(i)) &lt;&lt; &quot; where i = &quot; &lt;&lt; i;</computeroutput>. It doesn&apos;t require a new set of macros or special functions.</para></listitem><listitem><para>Google Test automatically detects your tests and doesn&apos;t require you to enumerate them in order to run them.</para></listitem><listitem><para>Death tests are pretty handy for ensuring that your asserts in production code are triggered by the right conditions.</para></listitem><listitem><para><computeroutput>SCOPED_TRACE</computeroutput> helps you understand the context of an assertion failure when it comes from inside a sub-routine or loop.</para></listitem><listitem><para>You can decide which tests to run using name patterns. This saves time when you want to quickly reproduce a test failure.</para></listitem><listitem><para>Google Test can generate XML test result reports that can be parsed by popular continuous build system like Hudson.</para></listitem><listitem><para>Simple things are easy in Google Test, while hard things are possible: in addition to advanced features like <ulink url="AdvancedGuide.md#global-set-up-and-tear-down">global test environments</ulink> and tests parameterized by <ulink url="AdvancedGuide.md#value-parameterized-tests">values</ulink> or <ulink url="docs/AdvancedGuide.md#typed-tests">types</ulink>, Google Test supports various ways for the user to extend the framework <ndash/> if Google Test doesn&apos;t do something out of the box, chances are that a user can implement the feature using Google Test&apos;s public API, without changing Google Test itself. In particular, you can:<itemizedlist>
<listitem><para>expand your testing vocabulary by defining <ulink url="AdvancedGuide.md#predicate-assertions-for-better-error-messages">custom predicates</ulink>,</para></listitem><listitem><para>teach Google Test how to <ulink url="AdvancedGuide.md#teaching-google-test-how-to-print-your-values">print your types</ulink>,</para></listitem><listitem><para>define your own testing macros or utilities and verify them using Google Test&apos;s <ulink url="AdvancedGuide.md#catching-failures">Service Provider Interface</ulink>, and</para></listitem><listitem><para>reflect on the test cases or change the test output format by intercepting the <ulink url="AdvancedGuide.md#extending-google-test-by-handling-test-events">test events</ulink>.</para></listitem></itemizedlist>
</para></listitem></itemizedlist>
</para><para><heading level="2">I&apos;m getting warnings when compiling Google Test. Would you fix them?</heading>
</para><para>We strive to minimize compiler warnings Google Test generates. Before releasing a new version, we test to make sure that it doesn&apos;t generate warnings when compiled using its CMake script on Windows, Linux, and Mac OS.</para><para>Unfortunately, this doesn&apos;t mean you are guaranteed to see no warnings when compiling Google Test in your environment:</para><para><itemizedlist>
<listitem><para>You may be using a different compiler as we use, or a different version of the same compiler. We cannot possibly test for all compilers.</para></listitem><listitem><para>You may be compiling on a different platform as we do.</para></listitem><listitem><para>Your project may be using different compiler flags as we do.</para></listitem></itemizedlist>
</para><para>It is not always possible to make Google Test warning-free for everyone. Or, it may not be desirable if the warning is rarely enabled and fixing the violations makes the code more complex.</para><para>If you see warnings when compiling Google Test, we suggest that you use the <computeroutput>-isystem</computeroutput> flag (assuming your are using GCC) to mark Google Test headers as system headers. That&apos;ll suppress warnings from Google Test headers.</para><para><heading level="2">Why should not test case names and test names contain underscore?</heading>
</para><para>Underscore (<computeroutput>_</computeroutput>) is special, as C++ reserves the following to be used by the compiler and the standard library:</para><para><orderedlist>
<listitem><para>any identifier that starts with an <computeroutput>_</computeroutput> followed by an upper-case letter, and</para></listitem></orderedlist>
<orderedlist>
<listitem><para>any identifier that containers two consecutive underscores (i.e. <computeroutput>__</computeroutput>) <emphasis>anywhere</emphasis> in its name.</para></listitem></orderedlist>
</para><para>User code is <emphasis>prohibited</emphasis> from using such identifiers.</para><para>Now let&apos;s look at what this means for <computeroutput>TEST</computeroutput> and <computeroutput>TEST_F</computeroutput>.</para><para>Currently <computeroutput>TEST(TestCaseName, TestName)</computeroutput> generates a class named <computeroutput>TestCaseName_TestName_Test</computeroutput>. What happens if <computeroutput>TestCaseName</computeroutput> or <computeroutput>TestName</computeroutput> contains <computeroutput>_</computeroutput>?</para><para><orderedlist>
<listitem><para>If <computeroutput>TestCaseName</computeroutput> starts with an <computeroutput>_</computeroutput> followed by an upper-case letter (say, <computeroutput>_Foo</computeroutput>), we end up with <computeroutput>_Foo_TestName_Test</computeroutput>, which is reserved and thus invalid.</para></listitem></orderedlist>
<orderedlist>
<listitem><para>If <computeroutput>TestCaseName</computeroutput> ends with an <computeroutput>_</computeroutput> (say, <computeroutput>Foo_</computeroutput>), we get <computeroutput>Foo__TestName_Test</computeroutput>, which is invalid.</para></listitem></orderedlist>
<orderedlist>
<listitem><para>If <computeroutput>TestName</computeroutput> starts with an <computeroutput>_</computeroutput> (say, <computeroutput>_Bar</computeroutput>), we get <computeroutput>TestCaseName__Bar_Test</computeroutput>, which is invalid.</para></listitem></orderedlist>
<orderedlist>
<listitem><para>If <computeroutput>TestName</computeroutput> ends with an <computeroutput>_</computeroutput> (say, <computeroutput>Bar_</computeroutput>), we get <computeroutput>TestCaseName_Bar__Test</computeroutput>, which is invalid.</para></listitem></orderedlist>
</para><para>So clearly <computeroutput>TestCaseName</computeroutput> and <computeroutput>TestName</computeroutput> cannot start or end with <computeroutput>_</computeroutput> (Actually, <computeroutput>TestCaseName</computeroutput> can start with <computeroutput>_</computeroutput> <ndash/> as long as the <computeroutput>_</computeroutput> isn&apos;t followed by an upper-case letter. But that&apos;s getting complicated. So for simplicity we just say that it cannot start with <computeroutput>_</computeroutput>.).</para><para>It may seem fine for <computeroutput>TestCaseName</computeroutput> and <computeroutput>TestName</computeroutput> to contain <computeroutput>_</computeroutput> in the middle. However, consider this: <programlisting><codeline><highlight class="normal">TEST(Time,<sp/>Flies_Like_An_Arrow)<sp/>{<sp/>...<sp/>}</highlight></codeline>
<codeline><highlight class="normal">TEST(Time_Flies,<sp/>Like_An_Arrow)<sp/>{<sp/>...<sp/>}</highlight></codeline>
</programlisting></para><para>Now, the two <computeroutput>TEST</computeroutput>s will both generate the same class (<computeroutput>Time_Files_Like_An_Arrow_Test</computeroutput>). That&apos;s not good.</para><para>So for simplicity, we just ask the users to avoid <computeroutput>_</computeroutput> in <computeroutput>TestCaseName</computeroutput> and <computeroutput>TestName</computeroutput>. The rule is more constraining than necessary, but it&apos;s simple and easy to remember. It also gives Google Test some wiggle room in case its implementation needs to change in the future.</para><para>If you violate the rule, there may not be immediately consequences, but your test may (just may) break with a new compiler (or a new version of the compiler you are using) or with a new version of Google Test. Therefore it&apos;s best to follow the rule.</para><para><heading level="2">Why is it not recommended to install a pre-compiled copy of Google Test (for example, into /usr/local)?</heading>
</para><para>In the early days, we said that you could install compiled Google Test libraries on <computeroutput>*</computeroutput>nix systems using <computeroutput>make install</computeroutput>. Then every user of your machine can write tests without recompiling Google Test.</para><para>This seemed like a good idea, but it has a got-cha: every user needs to compile his tests using the <emphasis>same</emphasis> compiler flags used to compile the installed Google Test libraries; otherwise he may run into undefined behaviors (i.e. the tests can behave strangely and may even crash for no obvious reasons).</para><para>Why? Because C++ has this thing called the One-Definition Rule: if two C++ source files contain different definitions of the same class/function/variable, and you link them together, you violate the rule. The linker may or may not catch the error (in many cases it&apos;s not required by the C++ standard to catch the violation). If it doesn&apos;t, you get strange run-time behaviors that are unexpected and hard to debug.</para><para>If you compile Google Test and your test code using different compiler flags, they may see different definitions of the same class/function/variable (e.g. due to the use of <computeroutput>#if</computeroutput> in Google Test). Therefore, for your sanity, we recommend to avoid installing pre-compiled Google Test libraries. Instead, each project should compile Google Test itself such that it can be sure that the same flags are used for both Google Test and the tests.</para><para><heading level="2">How do I generate 64-bit binaries on Windows (using Visual Studio 2008)?</heading>
</para><para>(Answered by Trevor Robinson)</para><para>Load the supplied Visual Studio solution file, either <computeroutput>msvc\gtest-md.sln</computeroutput> or <computeroutput>msvc\gtest.sln</computeroutput>. Go through the migration wizard to migrate the solution and project files to Visual Studio 2008. Select <computeroutput>Configuration Manager...</computeroutput> from the <computeroutput>Build</computeroutput> menu. Select <computeroutput>&lt;New...&gt;</computeroutput> from the <computeroutput>Active solution platform</computeroutput> dropdown. Select <computeroutput>x64</computeroutput> from the new platform dropdown, leave <computeroutput>Copy settings from</computeroutput> set to <computeroutput>Win32</computeroutput> and <computeroutput>Create new project platforms</computeroutput> checked, then click <computeroutput>OK</computeroutput>. You now have <computeroutput>Win32</computeroutput> and <computeroutput>x64</computeroutput> platform configurations, selectable from the <computeroutput>Standard</computeroutput> toolbar, which allow you to toggle between building 32-bit or 64-bit binaries (or both at once using Batch Build).</para><para>In order to prevent build output files from overwriting one another, you&apos;ll need to change the <computeroutput>Intermediate Directory</computeroutput> settings for the newly created platform configuration across all the projects. To do this, multi-select (e.g. using shift-click) all projects (but not the solution) in the <computeroutput>Solution Explorer</computeroutput>. Right-click one of them and select <computeroutput>Properties</computeroutput>. In the left pane, select <computeroutput>Configuration Properties</computeroutput>, and from the <computeroutput>Configuration</computeroutput> dropdown, select <computeroutput>All Configurations</computeroutput>. Make sure the selected platform is <computeroutput>x64</computeroutput>. For the <computeroutput>Intermediate Directory</computeroutput> setting, change the value from <computeroutput>\</computeroutput> to <computeroutput>\</computeroutput>. Click <computeroutput>OK</computeroutput> and then build the solution. When the build is complete, the 64-bit binaries will be in the <computeroutput>msvc\x64\Debug</computeroutput> directory.</para><para><heading level="2">Can I use Google Test on MinGW?</heading>
</para><para>We haven&apos;t tested this ourselves, but Per Abrahamsen reported that he was able to compile and install Google Test successfully when using MinGW from Cygwin. You&apos;ll need to configure it with:</para><para><computeroutput>PATH/TO/configure CC=&quot;gcc -mno-cygwin&quot; CXX=&quot;g++ -mno-cygwin&quot;</computeroutput></para><para>You should be able to replace the <computeroutput>-mno-cygwin</computeroutput> option with direct links to the real MinGW binaries, but we haven&apos;t tried that.</para><para>Caveats:</para><para><itemizedlist>
<listitem><para>There are many warnings when compiling.</para></listitem><listitem><para><computeroutput>make check</computeroutput> will produce some errors as not all tests for Google Test itself are compatible with MinGW.</para></listitem></itemizedlist>
</para><para>We also have reports on successful cross compilation of Google Test MinGW binaries on Linux using <ulink url="http://wiki.wxwidgets.org/Cross-Compiling_Under_Linux#Cross-compiling_under_Linux_for_MS_Windows">these instructions</ulink> on the WxWidgets site.</para><para>Please contact <computeroutput>googletestframework@googlegroups.com</computeroutput> if you are interested in improving the support for MinGW.</para><para><heading level="2">Why does Google Test support EXPECT_EQ(NULL, ptr) and ASSERT_EQ(NULL, ptr) but not EXPECT_NE(NULL, ptr) and ASSERT_NE(NULL, ptr)?</heading>
</para><para>Due to some peculiarity of C++, it requires some non-trivial template meta programming tricks to support using <computeroutput>NULL</computeroutput> as an argument of the <computeroutput>EXPECT_XX()</computeroutput> and <computeroutput>ASSERT_XX()</computeroutput> macros. Therefore we only do it where it&apos;s most needed (otherwise we make the implementation of Google Test harder to maintain and more error-prone than necessary).</para><para>The <computeroutput>EXPECT_EQ()</computeroutput> macro takes the <emphasis>expected</emphasis> value as its first argument and the <emphasis>actual</emphasis> value as the second. It&apos;s reasonable that someone wants to write <computeroutput>EXPECT_EQ(NULL, some_expression)</computeroutput>, and this indeed was requested several times. Therefore we implemented it.</para><para>The need for <computeroutput>EXPECT_NE(NULL, ptr)</computeroutput> isn&apos;t nearly as strong. When the assertion fails, you already know that <computeroutput>ptr</computeroutput> must be <computeroutput>NULL</computeroutput>, so it doesn&apos;t add any information to print ptr in this case. That means <computeroutput>EXPECT_TRUE(ptr != NULL)</computeroutput> works just as well.</para><para>If we were to support <computeroutput>EXPECT_NE(NULL, ptr)</computeroutput>, for consistency we&apos;ll have to support <computeroutput>EXPECT_NE(ptr, NULL)</computeroutput> as well, as unlike <computeroutput>EXPECT_EQ</computeroutput>, we don&apos;t have a convention on the order of the two arguments for <computeroutput>EXPECT_NE</computeroutput>. This means using the template meta programming tricks twice in the implementation, making it even harder to understand and maintain. We believe the benefit doesn&apos;t justify the cost.</para><para>Finally, with the growth of Google <ref refid="class_mock" kindref="compound">Mock</ref>&apos;s <ulink url="../../googlemock/docs/CookBook.md#using-matchers-in-google-test-assertions">matcher</ulink> library, we are encouraging people to use the unified <computeroutput>EXPECT_THAT(value, matcher)</computeroutput> syntax more often in tests. One significant advantage of the matcher approach is that matchers can be easily combined to form new matchers, while the <computeroutput>EXPECT_NE</computeroutput>, etc, macros cannot be easily combined. Therefore we want to invest more in the matchers than in the <computeroutput>EXPECT_XX()</computeroutput> macros.</para><para><heading level="2">Does Google Test support running tests in parallel?</heading>
</para><para>Test runners tend to be tightly coupled with the build/test environment, and Google Test doesn&apos;t try to solve the problem of running tests in parallel. Instead, we tried to make Google Test work nicely with test runners. For example, Google Test&apos;s XML report contains the time spent on each test, and its <computeroutput>gtest_list_tests</computeroutput> and <computeroutput>gtest_filter</computeroutput> flags can be used for splitting the execution of test methods into multiple processes. These functionalities can help the test runner run the tests in parallel.</para><para><heading level="2">Why don&apos;t Google Test run the tests in different threads to speed things up?</heading>
</para><para>It&apos;s difficult to write thread-safe code. Most tests are not written with thread-safety in mind, and thus may not work correctly in a multi-threaded setting.</para><para>If you think about it, it&apos;s already hard to make your code work when you know what other threads are doing. It&apos;s much harder, and sometimes even impossible, to make your code work when you don&apos;t know what other threads are doing (remember that test methods can be added, deleted, or modified after your test was written). If you want to run the tests in parallel, you&apos;d better run them in different processes.</para><para><heading level="2">Why aren&apos;t Google Test assertions implemented using exceptions?</heading>
</para><para>Our original motivation was to be able to use Google Test in projects that disable exceptions. Later we realized some additional benefits of this approach:</para><para><orderedlist>
<listitem><para>Throwing in a destructor is undefined behavior in C++. Not using exceptions means Google Test&apos;s assertions are safe to use in destructors.</para></listitem></orderedlist>
<orderedlist>
<listitem><para>The <computeroutput>EXPECT_*</computeroutput> family of macros will continue even after a failure, allowing multiple failures in a <computeroutput>TEST</computeroutput> to be reported in a single run. This is a popular feature, as in C++ the edit-compile-test cycle is usually quite long and being able to fixing more than one thing at a time is a blessing.</para></listitem></orderedlist>
<orderedlist>
<listitem><para>If assertions are implemented using exceptions, a test may falsely ignore a failure if it&apos;s caught by user code: <programlisting><codeline><highlight class="normal">try<sp/>{<sp/>...<sp/>ASSERT_TRUE(...)<sp/>...<sp/>}</highlight></codeline>
<codeline><highlight class="normal">catch<sp/>(...)<sp/>{<sp/>...<sp/>}</highlight></codeline>
</programlisting> The above code will pass even if the <computeroutput>ASSERT_TRUE</computeroutput> throws. While it&apos;s unlikely for someone to write this in a test, it&apos;s possible to run into this pattern when you write assertions in callbacks that are called by the code under test.</para></listitem></orderedlist>
</para><para>The downside of not using exceptions is that <computeroutput>ASSERT_*</computeroutput> (implemented using <computeroutput>return</computeroutput>) will only abort the current function, not the current <computeroutput>TEST</computeroutput>.</para><para><heading level="2">Why do we use two different macros for tests with and without fixtures?</heading>
</para><para>Unfortunately, C++&apos;s macro system doesn&apos;t allow us to use the same macro for both cases. One possibility is to provide only one macro for tests with fixtures, and require the user to define an empty fixture sometimes:</para><para><programlisting><codeline><highlight class="normal">class<sp/>FooTest<sp/>:<sp/>public<sp/>::testing::Test<sp/>{};</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">TEST_F(FooTest,<sp/>DoesThis)<sp/>{<sp/>...<sp/>}</highlight></codeline>
</programlisting> or <programlisting><codeline><highlight class="normal">typedef<sp/>::testing::Test<sp/>FooTest;</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">TEST_F(FooTest,<sp/>DoesThat)<sp/>{<sp/>...<sp/>}</highlight></codeline>
</programlisting></para><para>Yet, many people think this is one line too many. :-) Our goal was to make it really easy to write tests, so we tried to make simple tests trivial to create. That means using a separate macro for such tests.</para><para>We think neither approach is ideal, yet either of them is reasonable. In the end, it probably doesn&apos;t matter much either way.</para><para><heading level="2">Why don&apos;t we use structs as test fixtures?</heading>
</para><para>We like to use structs only when representing passive data. This distinction between structs and classes is good for documenting the intent of the code&apos;s author. Since test fixtures have logic like <computeroutput>SetUp()</computeroutput> and <computeroutput>TearDown()</computeroutput>, they are better defined as classes.</para><para><heading level="2">Why are death tests implemented as assertions instead of using a test runner?</heading>
</para><para>Our goal was to make death tests as convenient for a user as C++ possibly allows. In particular:</para><para><itemizedlist>
<listitem><para>The runner-style requires to split the information into two pieces: the definition of the death test itself, and the specification for the runner on how to run the death test and what to expect. The death test would be written in C++, while the runner spec may or may not be. A user needs to carefully keep the two in sync. <computeroutput>ASSERT_DEATH(statement, expected_message)</computeroutput> specifies all necessary information in one place, in one language, without boilerplate code. It is very declarative.</para></listitem><listitem><para><computeroutput>ASSERT_DEATH</computeroutput> has a similar syntax and error-reporting semantics as other Google Test assertions, and thus is easy to learn.</para></listitem><listitem><para><computeroutput>ASSERT_DEATH</computeroutput> can be mixed with other assertions and other logic at your will. You are not limited to one death test per test method. For example, you can write something like: <programlisting><codeline><highlight class="normal">if<sp/>(FooCondition())<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>ASSERT_DEATH(Bar(),<sp/>&quot;blah&quot;);</highlight></codeline>
<codeline><highlight class="normal">}<sp/>else<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>ASSERT_EQ(5,<sp/>Bar());</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
</programlisting> If you prefer one death test per test method, you can write your tests in that style too, but we don&apos;t want to impose that on the users. The fewer artificial limitations the better.</para></listitem><listitem><para><computeroutput>ASSERT_DEATH</computeroutput> can reference local variables in the current function, and you can decide how many death tests you want based on run-time information. For example, <programlisting><codeline><highlight class="normal">const<sp/>int<sp/>count<sp/>=<sp/>GetCount();<sp/><sp/>//<sp/>Only<sp/>known<sp/>at<sp/>run<sp/>time.</highlight></codeline>
<codeline><highlight class="normal">for<sp/>(int<sp/>i<sp/>=<sp/>1;<sp/>i<sp/>&lt;=<sp/>count;<sp/>i++)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>ASSERT_DEATH({</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>double*<sp/>buffer<sp/>=<sp/>new<sp/>double[i];</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>...<sp/>initializes<sp/>buffer<sp/>...</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>Foo(buffer,<sp/>i)</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>},<sp/>&quot;blah<sp/>blah&quot;);</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
</programlisting> The runner-based approach tends to be more static and less flexible, or requires more user effort to get this kind of flexibility.</para></listitem></itemizedlist>
</para><para>Another interesting thing about <computeroutput>ASSERT_DEATH</computeroutput> is that it calls <computeroutput>fork()</computeroutput> to create a child process to run the death test. This is lightening fast, as <computeroutput>fork()</computeroutput> uses copy-on-write pages and incurs almost zero overhead, and the child process starts from the user-supplied statement directly, skipping all global and local initialization and any code leading to the given statement. If you launch the child process from scratch, it can take seconds just to load everything and start running if the test links to many libraries dynamically.</para><para><heading level="2">My death test modifies some state, but the change seems lost after the death test finishes. Why?</heading>
</para><para>Death tests (<computeroutput>EXPECT_DEATH</computeroutput>, etc) are executed in a sub-process s.t. the expected crash won&apos;t kill the test program (i.e. the parent process). As a result, any in-memory side effects they incur are observable in their respective sub-processes, but not in the parent process. You can think of them as running in a parallel universe, more or less.</para><para><heading level="2">The compiler complains about &quot;undefined references&quot; to some static const member variables, but I did define them in the class body. What&apos;s wrong?</heading>
</para><para>If your class has a static data member:</para><para><programlisting><codeline><highlight class="normal">//<sp/>foo.h</highlight></codeline>
<codeline><highlight class="normal">class<sp/>Foo<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>...</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>static<sp/>const<sp/>int<sp/>kBar<sp/>=<sp/>100;</highlight></codeline>
<codeline><highlight class="normal">};</highlight></codeline>
</programlisting></para><para>You also need to define it <emphasis>outside</emphasis> of the class body in <computeroutput>foo.cc</computeroutput>:</para><para><programlisting><codeline><highlight class="normal">const<sp/>int<sp/>Foo::kBar;<sp/><sp/>//<sp/>No<sp/>initializer<sp/>here.</highlight></codeline>
</programlisting></para><para>Otherwise your code is **invalid C++**, and may break in unexpected ways. In particular, using it in Google Test comparison assertions (<computeroutput>EXPECT_EQ</computeroutput>, etc) will generate an &quot;undefined reference&quot; linker error.</para><para><heading level="2">I have an interface that has several implementations. Can I write a set of tests once and repeat them over all the implementations?</heading>
</para><para>Google Test doesn&apos;t yet have good support for this kind of tests, or data-driven tests in general. We hope to be able to make improvements in this area soon.</para><para><heading level="2">Can I derive a test fixture from another?</heading>
</para><para>Yes.</para><para>Each test fixture has a corresponding and same named test case. This means only one test case can use a particular fixture. Sometimes, however, multiple test cases may want to use the same or slightly different fixtures. For example, you may want to make sure that all of a GUI library&apos;s test cases don&apos;t leak important system resources like fonts and brushes.</para><para>In Google Test, you share a fixture among test cases by putting the shared logic in a base test fixture, then deriving from that base a separate fixture for each test case that wants to use this common logic. You then use <computeroutput><ref refid="test__tracker_8cpp_1a567724948cdce4788a1e8c8baf6b6cfe" kindref="member">TEST_F()</ref></computeroutput> to write tests using each derived fixture.</para><para>Typically, your code looks like this:</para><para><programlisting><codeline><highlight class="normal">//<sp/>Defines<sp/>a<sp/>base<sp/>test<sp/>fixture.</highlight></codeline>
<codeline><highlight class="normal">class<sp/>BaseTest<sp/>:<sp/>public<sp/>::testing::Test<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>protected:</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/>...</highlight></codeline>
<codeline><highlight class="normal">};</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">//<sp/>Derives<sp/>a<sp/>fixture<sp/>FooTest<sp/>from<sp/>BaseTest.</highlight></codeline>
<codeline><highlight class="normal">class<sp/>FooTest<sp/>:<sp/>public<sp/>BaseTest<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>protected:</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>virtual<sp/>void<sp/>SetUp()<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/>BaseTest::SetUp();<sp/><sp/>//<sp/>Sets<sp/>up<sp/>the<sp/>base<sp/>fixture<sp/>first.</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/>...<sp/>additional<sp/>set-up<sp/>work<sp/>...</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>}</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>virtual<sp/>void<sp/>TearDown()<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/>...<sp/>clean-up<sp/>work<sp/>for<sp/>FooTest<sp/>...</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/>BaseTest::TearDown();<sp/><sp/>//<sp/>Remember<sp/>to<sp/>tear<sp/>down<sp/>the<sp/>base<sp/>fixture</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/>//<sp/>after<sp/>cleaning<sp/>up<sp/>FooTest!</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>}</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>...<sp/>functions<sp/>and<sp/>variables<sp/>for<sp/>FooTest<sp/>...</highlight></codeline>
<codeline><highlight class="normal">};</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">//<sp/>Tests<sp/>that<sp/>use<sp/>the<sp/>fixture<sp/>FooTest.</highlight></codeline>
<codeline><highlight class="normal">TEST_F(FooTest,<sp/>Bar)<sp/>{<sp/>...<sp/>}</highlight></codeline>
<codeline><highlight class="normal">TEST_F(FooTest,<sp/>Baz)<sp/>{<sp/>...<sp/>}</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">...<sp/>additional<sp/>fixtures<sp/>derived<sp/>from<sp/>BaseTest<sp/>...</highlight></codeline>
</programlisting></para><para>If necessary, you can continue to derive test fixtures from a derived fixture. Google Test has no limit on how deep the hierarchy can be.</para><para>For a complete example using derived test fixtures, see <ulink url="../samples/sample5_unittest.cc">sample5</ulink>.</para><para><heading level="2">My compiler complains &quot;void value not ignored as it ought to be.&quot; What does this mean?</heading>
</para><para>You&apos;re probably using an <computeroutput>ASSERT_*()</computeroutput> in a function that doesn&apos;t return <computeroutput>void</computeroutput>. <computeroutput>ASSERT_*()</computeroutput> can only be used in <computeroutput>void</computeroutput> functions.</para><para><heading level="2">My death test hangs (or seg-faults). How do I fix it?</heading>
</para><para>In Google Test, death tests are run in a child process and the way they work is delicate. To write death tests you really need to understand how they work. Please make sure you have read this.</para><para>In particular, death tests don&apos;t like having multiple threads in the parent process. So the first thing you can try is to eliminate creating threads outside of <computeroutput>EXPECT_DEATH()</computeroutput>.</para><para>Sometimes this is impossible as some library you must use may be creating threads before <computeroutput>main()</computeroutput> is even reached. In this case, you can try to minimize the chance of conflicts by either moving as many activities as possible inside <computeroutput>EXPECT_DEATH()</computeroutput> (in the extreme case, you want to move everything inside), or leaving as few things as possible in it. Also, you can try to set the death test style to <computeroutput>&quot;threadsafe&quot;</computeroutput>, which is safer but slower, and see if it helps.</para><para>If you go with thread-safe death tests, remember that they rerun the test program from the beginning in the child process. Therefore make sure your program can run side-by-side with itself and is deterministic.</para><para>In the end, this boils down to good concurrent programming. You have to make sure that there is no race conditions or dead locks in your program. No silver bullet - sorry!</para><para><heading level="2">Should I use the constructor/destructor of the test fixture or the set-up/tear-down function?</heading>
</para><para>The first thing to remember is that Google Test does not reuse the same test fixture object across multiple tests. For each <computeroutput>TEST_F</computeroutput>, Google Test will create a fresh test fixture object, <emphasis>immediately</emphasis> call <computeroutput>SetUp()</computeroutput>, run the test body, call <computeroutput>TearDown()</computeroutput>, and then <emphasis>immediately</emphasis> delete the test fixture object.</para><para>When you need to write per-test set-up and tear-down logic, you have the choice between using the test fixture constructor/destructor or <computeroutput>SetUp()/TearDown()</computeroutput>. The former is usually preferred, as it has the following benefits:</para><para><itemizedlist>
<listitem><para>By initializing a member variable in the constructor, we have the option to make it <computeroutput>const</computeroutput>, which helps prevent accidental changes to its value and makes the tests more obviously correct.</para></listitem><listitem><para>In case we need to subclass the test fixture class, the subclass&apos; constructor is guaranteed to call the base class&apos; constructor first, and the subclass&apos; destructor is guaranteed to call the base class&apos; destructor afterward. With <computeroutput>SetUp()/TearDown()</computeroutput>, a subclass may make the mistake of forgetting to call the base class&apos; <computeroutput>SetUp()/TearDown()</computeroutput> or call them at the wrong moment.</para></listitem></itemizedlist>
</para><para>You may still want to use <computeroutput>SetUp()/TearDown()</computeroutput> in the following rare cases:<itemizedlist>
<listitem><para>If the tear-down operation could throw an exception, you must use <computeroutput>TearDown()</computeroutput> as opposed to the destructor, as throwing in a destructor leads to undefined behavior and usually will kill your program right away. Note that many standard libraries (like STL) may throw when exceptions are enabled in the compiler. Therefore you should prefer <computeroutput>TearDown()</computeroutput> if you want to write portable tests that work with or without exceptions.</para></listitem><listitem><para>The assertion macros throw an exception when flag <computeroutput>--gtest_throw_on_failure</computeroutput> is specified. Therefore, you shouldn&apos;t use Google Test assertions in a destructor if you plan to run your tests with this flag.</para></listitem><listitem><para>In a constructor or destructor, you cannot make a virtual function call on this object. (You can call a method declared as virtual, but it will be statically bound.) Therefore, if you need to call a method that will be overriden in a derived class, you have to use <computeroutput>SetUp()/TearDown()</computeroutput>.</para></listitem></itemizedlist>
</para><para><heading level="2">The compiler complains &quot;no matching function to call&quot; when I use ASSERT_PREDn. How do I fix it?</heading>
</para><para>If the predicate function you use in <computeroutput>ASSERT_PRED*</computeroutput> or <computeroutput>EXPECT_PRED*</computeroutput> is overloaded or a template, the compiler will have trouble figuring out which overloaded version it should use. <computeroutput>ASSERT_PRED_FORMAT*</computeroutput> and <computeroutput>EXPECT_PRED_FORMAT*</computeroutput> don&apos;t have this problem.</para><para>If you see this error, you might want to switch to <computeroutput>(ASSERT|EXPECT)_PRED_FORMAT*</computeroutput>, which will also give you a better failure message. If, however, that is not an option, you can resolve the problem by explicitly telling the compiler which version to pick.</para><para>For example, suppose you have</para><para><programlisting><codeline><highlight class="normal">bool<sp/>IsPositive(int<sp/>n)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>return<sp/>n<sp/>&gt;<sp/>0;</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
<codeline><highlight class="normal">bool<sp/>IsPositive(double<sp/>x)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>return<sp/>x<sp/>&gt;<sp/>0;</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
</programlisting></para><para>you will get a compiler error if you write</para><para><programlisting><codeline><highlight class="normal">EXPECT_PRED1(IsPositive,<sp/>5);</highlight></codeline>
</programlisting></para><para>However, this will work:</para><para><programlisting><codeline><highlight class="normal">EXPECT_PRED1(*static_cast&lt;bool<sp/>(*)(int)&gt;*(IsPositive),<sp/>5);</highlight></codeline>
</programlisting></para><para>(The stuff inside the angled brackets for the <computeroutput>static_cast</computeroutput> operator is the type of the function pointer for the <computeroutput>int</computeroutput>-version of <computeroutput>IsPositive()</computeroutput>.)</para><para>As another example, when you have a template function</para><para><programlisting><codeline><highlight class="normal">template<sp/>&lt;typename<sp/>T&gt;</highlight></codeline>
<codeline><highlight class="normal">bool<sp/>IsNegative(T<sp/>x)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>return<sp/>x<sp/>&lt;<sp/>0;</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
</programlisting></para><para>you can use it in a predicate assertion like this:</para><para><programlisting><codeline><highlight class="normal">ASSERT_PRED1(IsNegative*&lt;int&gt;*,<sp/>-5);</highlight></codeline>
</programlisting></para><para>Things are more interesting if your template has more than one parameters. The following won&apos;t compile:</para><para><programlisting><codeline><highlight class="normal">ASSERT_PRED2(*GreaterThan&lt;int,<sp/>int&gt;*,<sp/>5,<sp/>0);</highlight></codeline>
</programlisting></para><para>as the C++ pre-processor thinks you are giving <computeroutput>ASSERT_PRED2</computeroutput> 4 arguments, which is one more than expected. The workaround is to wrap the predicate function in parentheses:</para><para><programlisting><codeline><highlight class="normal">ASSERT_PRED2(*(GreaterThan&lt;int,<sp/>int&gt;)*,<sp/>5,<sp/>0);</highlight></codeline>
</programlisting></para><para><heading level="2">My compiler complains about &quot;ignoring return value&quot; when I call RUN_ALL_TESTS(). Why?</heading>
</para><para>Some people had been ignoring the return value of <computeroutput>RUN_ALL_TESTS()</computeroutput>. That is, instead of</para><para><programlisting><codeline><highlight class="normal">return<sp/>RUN_ALL_TESTS();</highlight></codeline>
</programlisting></para><para>they write</para><para><programlisting><codeline><highlight class="normal">RUN_ALL_TESTS();</highlight></codeline>
</programlisting></para><para>This is wrong and dangerous. A test runner needs to see the return value of <computeroutput>RUN_ALL_TESTS()</computeroutput> in order to determine if a test has passed. If your <computeroutput>main()</computeroutput> function ignores it, your test will be considered successful even if it has a Google Test assertion failure. Very bad.</para><para>To help the users avoid this dangerous bug, the implementation of <computeroutput>RUN_ALL_TESTS()</computeroutput> causes gcc to raise this warning, when the return value is ignored. If you see this warning, the fix is simple: just make sure its value is used as the return value of <computeroutput>main()</computeroutput>.</para><para><heading level="2">My compiler complains that a constructor (or destructor) cannot return a value. What&apos;s going on?</heading>
</para><para>Due to a peculiarity of C++, in order to support the syntax for streaming messages to an <computeroutput>ASSERT_*</computeroutput>, e.g.</para><para><programlisting><codeline><highlight class="normal">ASSERT_EQ(1,<sp/>Foo())<sp/>&lt;&lt;<sp/>&quot;blah<sp/>blah&quot;<sp/>&lt;&lt;<sp/>foo;</highlight></codeline>
</programlisting></para><para>we had to give up using <computeroutput>ASSERT*</computeroutput> and <computeroutput>FAIL*</computeroutput> (but not <computeroutput>EXPECT*</computeroutput> and <computeroutput>ADD_FAILURE*</computeroutput>) in constructors and destructors. The workaround is to move the content of your constructor/destructor to a private void member function, or switch to <computeroutput>EXPECT_*()</computeroutput> if that works. This section in the user&apos;s guide explains it.</para><para><heading level="2">My set-up function is not called. Why?</heading>
</para><para>C++ is case-sensitive. It should be spelled as <computeroutput>SetUp()</computeroutput>. Did you spell it as <computeroutput>Setup()</computeroutput>?</para><para>Similarly, sometimes people spell <computeroutput>SetUpTestCase()</computeroutput> as <computeroutput>SetupTestCase()</computeroutput> and wonder why it&apos;s never called.</para><para><heading level="2">How do I jump to the line of a failure in Emacs directly?</heading>
</para><para>Google Test&apos;s failure message format is understood by Emacs and many other IDEs, like acme and XCode. If a Google Test message is in a compilation buffer in Emacs, then it&apos;s clickable. You can now hit <computeroutput>enter</computeroutput> on a message to jump to the corresponding source code, or use <computeroutput>C-x</computeroutput>` to jump to the next failure.</para><para><heading level="2">I have several test cases which share the same test fixture logic, do I have to define a new test fixture class for each of them? This seems pretty tedious.</heading>
</para><para>You don&apos;t have to. Instead of</para><para><programlisting><codeline><highlight class="normal">class<sp/>FooTest<sp/>:<sp/>public<sp/>BaseTest<sp/>{};</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">TEST_F(FooTest,<sp/>Abc)<sp/>{<sp/>...<sp/>}</highlight></codeline>
<codeline><highlight class="normal">TEST_F(FooTest,<sp/>Def)<sp/>{<sp/>...<sp/>}</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">class<sp/>BarTest<sp/>:<sp/>public<sp/>BaseTest<sp/>{};</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">TEST_F(BarTest,<sp/>Abc)<sp/>{<sp/>...<sp/>}</highlight></codeline>
<codeline><highlight class="normal">TEST_F(BarTest,<sp/>Def)<sp/>{<sp/>...<sp/>}</highlight></codeline>
</programlisting></para><para>you can simply <computeroutput>typedef</computeroutput> the test fixtures: <programlisting><codeline><highlight class="normal">typedef<sp/>BaseTest<sp/>FooTest;</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">TEST_F(FooTest,<sp/>Abc)<sp/>{<sp/>...<sp/>}</highlight></codeline>
<codeline><highlight class="normal">TEST_F(FooTest,<sp/>Def)<sp/>{<sp/>...<sp/>}</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">typedef<sp/>BaseTest<sp/>BarTest;</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">TEST_F(BarTest,<sp/>Abc)<sp/>{<sp/>...<sp/>}</highlight></codeline>
<codeline><highlight class="normal">TEST_F(BarTest,<sp/>Def)<sp/>{<sp/>...<sp/>}</highlight></codeline>
</programlisting></para><para><heading level="2">The Google Test output is buried in a whole bunch of log messages. What do I do?</heading>
</para><para>The Google Test output is meant to be a concise and human-friendly report. If your test generates textual output itself, it will mix with the Google Test output, making it hard to read. However, there is an easy solution to this problem.</para><para>Since most log messages go to stderr, we decided to let Google Test output go to stdout. This way, you can easily separate the two using redirection. For example: <programlisting><codeline><highlight class="normal">./my_test<sp/>&gt;<sp/>googletest_output.txt</highlight></codeline>
</programlisting></para><para><heading level="2">Why should I prefer test fixtures over global variables?</heading>
</para><para>There are several good reasons:<orderedlist>
<listitem><para>It&apos;s likely your test needs to change the states of its global variables. This makes it difficult to keep side effects from escaping one test and contaminating others, making debugging difficult. By using fixtures, each test has a fresh set of variables that&apos;s different (but with the same names). Thus, tests are kept independent of each other.</para></listitem></orderedlist>
<orderedlist>
<listitem><para>Global variables pollute the global namespace.</para></listitem></orderedlist>
<orderedlist>
<listitem><para>Test fixtures can be reused via subclassing, which cannot be done easily with global variables. This is useful if many test cases have something in common.</para></listitem></orderedlist>
</para><para><heading level="2">How do I test private class members without writing FRIEND_TEST()s?</heading>
</para><para>You should try to write testable code, which means classes should be easily tested from their public interface. One way to achieve this is the Pimpl idiom: you move all private members of a class into a helper class, and make all members of the helper class public.</para><para>You have several other options that don&apos;t require using <computeroutput>FRIEND_TEST</computeroutput>:<itemizedlist>
<listitem><para>Write the tests as members of the fixture class: <programlisting><codeline><highlight class="normal">class<sp/>Foo<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>friend<sp/>class<sp/>FooTest;</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>...</highlight></codeline>
<codeline><highlight class="normal">};</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">class<sp/>FooTest<sp/>:<sp/>public<sp/>::testing::Test<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/>protected:</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>...</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>void<sp/>Test1()<sp/>{...}<sp/>//<sp/>This<sp/>accesses<sp/>private<sp/>members<sp/>of<sp/>class<sp/>Foo.</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>void<sp/>Test2()<sp/>{...}<sp/>//<sp/>So<sp/>does<sp/>this<sp/>one.</highlight></codeline>
<codeline><highlight class="normal">};</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">TEST_F(FooTest,<sp/>Test1)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>Test1();</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">TEST_F(FooTest,<sp/>Test2)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>Test2();</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
</programlisting></para></listitem><listitem><para>In the fixture class, write accessors for the tested class&apos; private members, then use the accessors in your tests: <programlisting><codeline><highlight class="normal">class<sp/>Foo<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>friend<sp/>class<sp/>FooTest;</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>...</highlight></codeline>
<codeline><highlight class="normal">};</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">class<sp/>FooTest<sp/>:<sp/>public<sp/>::testing::Test<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/>protected:</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>...</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>T1<sp/>get_private_member1(Foo*<sp/>obj)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>return<sp/>obj-&gt;private_member1_;</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>}</highlight></codeline>
<codeline><highlight class="normal">};</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">TEST_F(FooTest,<sp/>Test1)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>...</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>get_private_member1(x)</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>...</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
</programlisting></para></listitem><listitem><para>If the methods are declared <bold>protected</bold>, you can change their access level in a test-only subclass: <programlisting><codeline><highlight class="normal">class<sp/>YourClass<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>...</highlight></codeline>
<codeline><highlight class="normal"><sp/>protected:<sp/>//<sp/>protected<sp/>access<sp/>for<sp/>testability.</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>int<sp/>DoSomethingReturningInt();</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>...</highlight></codeline>
<codeline><highlight class="normal">};</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">//<sp/>in<sp/>the<sp/>your_class_test.cc<sp/>file:</highlight></codeline>
<codeline><highlight class="normal">class<sp/>TestableYourClass<sp/>:<sp/>public<sp/>YourClass<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>...</highlight></codeline>
<codeline><highlight class="normal"><sp/>public:<sp/>using<sp/>YourClass::DoSomethingReturningInt;<sp/>//<sp/>changes<sp/>access<sp/>rights</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>...</highlight></codeline>
<codeline><highlight class="normal">};</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">TEST_F(YourClassTest,<sp/>DoSomethingTest)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>TestableYourClass<sp/>obj;</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>assertEquals(expected_value,<sp/>obj.DoSomethingReturningInt());</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
</programlisting></para></listitem></itemizedlist>
</para><para><heading level="2">How do I test private class static members without writing FRIEND_TEST()s?</heading>
</para><para>We find private static methods clutter the header file. They are implementation details and ideally should be kept out of a .h. So often I make them free functions instead.</para><para>Instead of: <programlisting><codeline><highlight class="normal">//<sp/>foo.h</highlight></codeline>
<codeline><highlight class="normal">class<sp/>Foo<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>...</highlight></codeline>
<codeline><highlight class="normal"><sp/>private:</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>static<sp/>bool<sp/>Func(int<sp/>n);</highlight></codeline>
<codeline><highlight class="normal">};</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">//<sp/>foo.cc</highlight></codeline>
<codeline><highlight class="normal">bool<sp/>Foo::Func(int<sp/>n)<sp/>{<sp/>...<sp/>}</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">//<sp/>foo_test.cc</highlight></codeline>
<codeline><highlight class="normal">EXPECT_TRUE(Foo::Func(12345));</highlight></codeline>
</programlisting></para><para>You probably should better write: <programlisting><codeline><highlight class="normal">//<sp/>foo.h</highlight></codeline>
<codeline><highlight class="normal">class<sp/>Foo<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>...</highlight></codeline>
<codeline><highlight class="normal">};</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">//<sp/>foo.cc</highlight></codeline>
<codeline><highlight class="normal">namespace<sp/>internal<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>bool<sp/>Func(int<sp/>n)<sp/>{<sp/>...<sp/>}</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">//<sp/>foo_test.cc</highlight></codeline>
<codeline><highlight class="normal">namespace<sp/>internal<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>bool<sp/>Func(int<sp/>n);</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">EXPECT_TRUE(internal::Func(12345));</highlight></codeline>
</programlisting></para><para><heading level="2">I would like to run a test several times with different parameters. Do I need to write several similar copies of it?</heading>
</para><para>No. You can use a feature called <ulink url="AdvancedGuide.md#Value_Parameterized_Tests">value-parameterized tests</ulink> which lets you repeat your tests with different parameters, without defining it more than once.</para><para><heading level="2">How do I test a file that defines main()?</heading>
</para><para>To test a <computeroutput>foo.cc</computeroutput> file, you need to compile and link it into your unit test program. However, when the file contains a definition for the <computeroutput>main()</computeroutput> function, it will clash with the <computeroutput>main()</computeroutput> of your unit test, and will result in a build error.</para><para>The right solution is to split it into three files:<orderedlist>
<listitem><para><computeroutput>foo.h</computeroutput> which contains the declarations,</para></listitem></orderedlist>
<orderedlist>
<listitem><para><computeroutput>foo.cc</computeroutput> which contains the definitions except <computeroutput>main()</computeroutput>, and</para></listitem></orderedlist>
<orderedlist>
<listitem><para><computeroutput>foo_main.cc</computeroutput> which contains nothing but the definition of <computeroutput>main()</computeroutput>.</para></listitem></orderedlist>
</para><para>Then <computeroutput>foo.cc</computeroutput> can be easily tested.</para><para>If you are adding tests to an existing file and don&apos;t want an intrusive change like this, there is a hack: just include the entire <computeroutput>foo.cc</computeroutput> file in your unit test. For example: <programlisting><codeline><highlight class="normal">//<sp/>File<sp/>foo_unittest.cc</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">//<sp/>The<sp/>headers<sp/>section</highlight></codeline>
<codeline><highlight class="normal">...</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">//<sp/>Renames<sp/>main()<sp/>in<sp/>foo.cc<sp/>to<sp/>make<sp/>room<sp/>for<sp/>the<sp/>unit<sp/>test<sp/>main()</highlight></codeline>
<codeline><highlight class="normal">#define<sp/>main<sp/>FooMain</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">#include<sp/>&quot;a/b/foo.cc&quot;</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">//<sp/>The<sp/>tests<sp/>start<sp/>here.</highlight></codeline>
<codeline><highlight class="normal">...</highlight></codeline>
</programlisting></para><para>However, please remember this is a hack and should only be used as the last resort.</para><para><heading level="2">What can the statement argument in ASSERT_DEATH() be?</heading>
</para><para><computeroutput>ASSERT_DEATH(_statement_, _regex_)</computeroutput> (or any death assertion macro) can be used wherever <computeroutput>_statement_</computeroutput> is valid. So basically <computeroutput>_statement_</computeroutput> can be any C++ statement that makes sense in the current context. In particular, it can reference global and/or local variables, and can be:<itemizedlist>
<listitem><para>a simple function call (often the case),</para></listitem><listitem><para>a complex expression, or</para></listitem><listitem><para>a compound statement.</para></listitem></itemizedlist>
</para><para>Some examples are shown here:</para><para><programlisting><codeline><highlight class="normal">//<sp/>A<sp/>death<sp/>test<sp/>can<sp/>be<sp/>a<sp/>simple<sp/>function<sp/>call.</highlight></codeline>
<codeline><highlight class="normal">TEST(MyDeathTest,<sp/>FunctionCall)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>ASSERT_DEATH(Xyz(5),<sp/>&quot;Xyz<sp/>failed&quot;);</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">//<sp/>Or<sp/>a<sp/>complex<sp/>expression<sp/>that<sp/>references<sp/>variables<sp/>and<sp/>functions.</highlight></codeline>
<codeline><highlight class="normal">TEST(MyDeathTest,<sp/>ComplexExpression)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>const<sp/>bool<sp/>c<sp/>=<sp/>Condition();</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>ASSERT_DEATH((c<sp/>?<sp/>Func1(0)<sp/>:<sp/>object2.Method(&quot;test&quot;)),</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/>&quot;(Func1|Method)<sp/>failed&quot;);</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">//<sp/>Death<sp/>assertions<sp/>can<sp/>be<sp/>used<sp/>any<sp/>where<sp/>in<sp/>a<sp/>function.<sp/>In</highlight></codeline>
<codeline><highlight class="normal">//<sp/>particular,<sp/>they<sp/>can<sp/>be<sp/>inside<sp/>a<sp/>loop.</highlight></codeline>
<codeline><highlight class="normal">TEST(MyDeathTest,<sp/>InsideLoop)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>//<sp/>Verifies<sp/>that<sp/>Foo(0),<sp/>Foo(1),<sp/>...,<sp/>and<sp/>Foo(4)<sp/>all<sp/>die.</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>for<sp/>(int<sp/>i<sp/>=<sp/>0;<sp/>i<sp/>&lt;<sp/>5;<sp/>i++)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>EXPECT_DEATH_M(Foo(i),<sp/>&quot;Foo<sp/>has<sp/>\\d+<sp/>errors&quot;,</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/>::testing::Message()<sp/>&lt;&lt;<sp/>&quot;where<sp/>i<sp/>is<sp/>&quot;<sp/>&lt;&lt;<sp/>i);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>}</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">//<sp/>A<sp/>death<sp/>assertion<sp/>can<sp/>contain<sp/>a<sp/>compound<sp/>statement.</highlight></codeline>
<codeline><highlight class="normal">TEST(MyDeathTest,<sp/>CompoundStatement)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>//<sp/>Verifies<sp/>that<sp/>at<sp/>lease<sp/>one<sp/>of<sp/>Bar(0),<sp/>Bar(1),<sp/>...,<sp/>and</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>//<sp/>Bar(4)<sp/>dies.</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>ASSERT_DEATH({</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>for<sp/>(int<sp/>i<sp/>=<sp/>0;<sp/>i<sp/>&lt;<sp/>5;<sp/>i++)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/>Bar(i);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>}</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>},</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>&quot;Bar<sp/>has<sp/>\\d+<sp/>errors&quot;);}</highlight></codeline>
</programlisting></para><para><computeroutput>googletest_unittest.cc</computeroutput> contains more examples if you are interested.</para><para><heading level="2">What syntax does the regular expression in ASSERT_DEATH use?</heading>
</para><para>On POSIX systems, Google Test uses the POSIX Extended regular expression syntax (<ulink url="http://en.wikipedia.org/wiki/Regular_expression#POSIX_Extended_Regular_Expressions">http://en.wikipedia.org/wiki/Regular_expression#POSIX_Extended_Regular_Expressions</ulink>). On Windows, it uses a limited variant of regular expression syntax. For more details, see the <ulink url="AdvancedGuide.md#Regular_Expression_Syntax">regular expression syntax</ulink>.</para><para><heading level="2">I have a fixture class Foo, but TEST_F(Foo, Bar) gives me error &quot;no matching function for call to Foo::Foo()&quot;. Why?</heading>
</para><para>Google Test needs to be able to create objects of your test fixture class, so it must have a default constructor. Normally the compiler will define one for you. However, there are cases where you have to define your own:<itemizedlist>
<listitem><para>If you explicitly declare a non-default constructor for class <computeroutput>Foo</computeroutput>, then you need to define a default constructor, even if it would be empty.</para></listitem><listitem><para>If <computeroutput>Foo</computeroutput> has a const non-static data member, then you have to define the default constructor <emphasis>and</emphasis> initialize the const member in the initializer list of the constructor. (Early versions of <computeroutput>gcc</computeroutput> doesn&apos;t force you to initialize the const member. It&apos;s a bug that has been fixed in <computeroutput>gcc 4</computeroutput>.)</para></listitem></itemizedlist>
</para><para><heading level="2">Why does ASSERT_DEATH complain about previous threads that were already joined?</heading>
</para><para>With the Linux pthread library, there is no turning back once you cross the line from single thread to multiple threads. The first time you create a thread, a manager thread is created in addition, so you get 3, not 2, threads. Later when the thread you create joins the main thread, the thread count decrements by 1, but the manager thread will never be killed, so you still have 2 threads, which means you cannot safely run a death test.</para><para>The new NPTL thread library doesn&apos;t suffer from this problem, as it doesn&apos;t create a manager thread. However, if you don&apos;t control which machine your test runs on, you shouldn&apos;t depend on this.</para><para><heading level="2">Why does Google Test require the entire test case, instead of individual tests, to be named FOODeathTest when it uses ASSERT_DEATH?</heading>
</para><para>Google Test does not interleave tests from different test cases. That is, it runs all tests in one test case first, and then runs all tests in the next test case, and so on. Google Test does this because it needs to set up a test case before the first test in it is run, and tear it down afterwords. Splitting up the test case would require multiple set-up and tear-down processes, which is inefficient and makes the semantics unclean.</para><para>If we were to determine the order of tests based on test name instead of test case name, then we would have a problem with the following situation:</para><para><programlisting><codeline><highlight class="normal">TEST_F(FooTest,<sp/>AbcDeathTest)<sp/>{<sp/>...<sp/>}</highlight></codeline>
<codeline><highlight class="normal">TEST_F(FooTest,<sp/>Uvw)<sp/>{<sp/>...<sp/>}</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">TEST_F(BarTest,<sp/>DefDeathTest)<sp/>{<sp/>...<sp/>}</highlight></codeline>
<codeline><highlight class="normal">TEST_F(BarTest,<sp/>Xyz)<sp/>{<sp/>...<sp/>}</highlight></codeline>
</programlisting></para><para>Since <computeroutput>FooTest.AbcDeathTest</computeroutput> needs to run before <computeroutput>BarTest.Xyz</computeroutput>, and we don&apos;t interleave tests from different test cases, we need to run all tests in the <computeroutput><ref refid="class_foo_test" kindref="compound">FooTest</ref></computeroutput> case before running any test in the <computeroutput>BarTest</computeroutput> case. This contradicts with the requirement to run <computeroutput>BarTest.DefDeathTest</computeroutput> before <computeroutput>FooTest.Uvw</computeroutput>.</para><para><heading level="2">But I don&apos;t like calling my entire test case FOODeathTest when it contains both death tests and non-death tests. What do I do?</heading>
</para><para>You don&apos;t have to, but if you like, you may split up the test case into <computeroutput><ref refid="class_foo_test" kindref="compound">FooTest</ref></computeroutput> and <computeroutput>FooDeathTest</computeroutput>, where the names make it clear that they are related:</para><para><programlisting><codeline><highlight class="normal">class<sp/>FooTest<sp/>:<sp/>public<sp/>::testing::Test<sp/>{<sp/>...<sp/>};</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">TEST_F(FooTest,<sp/>Abc)<sp/>{<sp/>...<sp/>}</highlight></codeline>
<codeline><highlight class="normal">TEST_F(FooTest,<sp/>Def)<sp/>{<sp/>...<sp/>}</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">typedef<sp/>FooTest<sp/>FooDeathTest;</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">TEST_F(FooDeathTest,<sp/>Uvw)<sp/>{<sp/>...<sp/>EXPECT_DEATH(...)<sp/>...<sp/>}</highlight></codeline>
<codeline><highlight class="normal">TEST_F(FooDeathTest,<sp/>Xyz)<sp/>{<sp/>...<sp/>ASSERT_DEATH(...)<sp/>...<sp/>}</highlight></codeline>
</programlisting></para><para><heading level="2">The compiler complains about &quot;no match for &apos;operator&lt;&lt;&apos;&quot; when I use an assertion. What gives?</heading>
</para><para>If you use a user-defined type <computeroutput>FooType</computeroutput> in an assertion, you must make sure there is an <computeroutput>std::ostream&amp; operator&lt;&lt;(std::ostream&amp;, const FooType&amp;)</computeroutput> function defined such that we can print a value of <computeroutput>FooType</computeroutput>.</para><para>In addition, if <computeroutput>FooType</computeroutput> is declared in a name space, the <computeroutput>&lt;&lt;</computeroutput> operator also needs to be defined in the <emphasis>same</emphasis> name space.</para><para><heading level="2">How do I suppress the memory leak messages on Windows?</heading>
</para><para>Since the statically initialized Google Test singleton requires allocations on the heap, the Visual C++ memory leak detector will report memory leaks at the end of the program run. The easiest way to avoid this is to use the <computeroutput>_CrtMemCheckpoint</computeroutput> and <computeroutput>_CrtMemDumpAllObjectsSince</computeroutput> calls to not report any statically initialized heap objects. See MSDN for more details and additional heap check/debug routines.</para><para><heading level="2">I am building my project with Google Test in Visual Studio and all I&apos;m getting is a bunch of linker errors (or warnings). Help!</heading>
</para><para>You may get a number of the following linker error or warnings if you attempt to link your test project with the Google Test library when your project and the are not built using the same compiler settings.</para><para><itemizedlist>
<listitem><para>LNK2005: symbol already defined in object</para></listitem><listitem><para>LNK4217: locally defined symbol &apos;symbol&apos; imported in function &apos;function&apos;</para></listitem><listitem><para>LNK4049: locally defined symbol &apos;symbol&apos; imported</para></listitem></itemizedlist>
</para><para>The Google Test project (gtest.vcproj) has the Runtime Library option set to /MT (use multi-threaded static libraries, /MTd for debug). If your project uses something else, for example /MD (use multi-threaded DLLs, /MDd for debug), you need to change the setting in the Google Test project to match your project&apos;s.</para><para>To update this setting open the project properties in the Visual Studio IDE then select the branch Configuration Properties | C/C++ | Code Generation and change the option &quot;Runtime Library&quot;. You may also try using gtest-md.vcproj instead of gtest.vcproj.</para><para><heading level="2">I put my tests in a library and Google Test doesn&apos;t run them. What&apos;s happening?</heading>
</para><para>Have you read a <ulink url="Primer.md#important-note-for-visual-c-users">warning</ulink> on the Google Test Primer page?</para><para><heading level="2">I want to use Google Test with Visual Studio but don&apos;t know where to start.</heading>
</para><para>Many people are in your position and one of the posted his solution to our mailing list.</para><para><heading level="2">I am seeing compile errors mentioning std::type_traits when I try to use Google Test on Solaris.</heading>
</para><para>Google Test uses parts of the standard C++ library that SunStudio does not support. Our users reported success using alternative implementations. Try running the build after runing this commad:</para><para>`export CC=cc CXX=CC CXXFLAGS=&apos;-library=stlport4&apos;`</para><para><heading level="2">How can my code detect if it is running in a test?</heading>
</para><para>If you write code that sniffs whether it&apos;s running in a test and does different things accordingly, you are leaking test-only logic into production code and there is no easy way to ensure that the test-only code paths aren&apos;t run by mistake in production. Such cleverness also leads to <ulink url="http://en.wikipedia.org/wiki/Unusual_software_bug#Heisenbug">Heisenbugs</ulink>. Therefore we strongly advise against the practice, and Google Test doesn&apos;t provide a way to do it.</para><para>In general, the recommended way to cause the code to behave differently under test is <ulink url="http://jamesshore.com/Blog/Dependency-Injection-Demystified.html">dependency injection</ulink>. You can inject different functionality from the test and from the production code. Since your production code doesn&apos;t link in the for-test logic at all, there is no danger in accidentally running it.</para><para>However, if you <emphasis>really</emphasis>, <emphasis>really</emphasis>, <emphasis>really</emphasis> have no choice, and if you follow the rule of ending your test program names with <computeroutput>_test</computeroutput>, you can use the <emphasis>horrible</emphasis> hack of sniffing your executable name (<computeroutput>argv[0]</computeroutput> in <computeroutput>main()</computeroutput>) to know whether the code is under test.</para><para><heading level="2">Google Test defines a macro that clashes with one defined by another library. How do I deal with that?</heading>
</para><para>In C++, macros don&apos;t obey namespaces. Therefore two libraries that both define a macro of the same name will clash if you <computeroutput>#include</computeroutput> both definitions. In case a Google Test macro clashes with another library, you can force Google Test to rename its macro to avoid the conflict.</para><para>Specifically, if both Google Test and some other code define macro <computeroutput>FOO</computeroutput>, you can add <programlisting><codeline><highlight class="normal">-DGTEST_DONT_DEFINE_FOO=1</highlight></codeline>
</programlisting> to the compiler flags to tell Google Test to change the macro&apos;s name from <computeroutput>FOO</computeroutput> to <computeroutput>GTEST_FOO</computeroutput>. For example, with <computeroutput>-DGTEST_DONT_DEFINE_TEST=1</computeroutput>, you&apos;ll need to write <programlisting><codeline><highlight class="normal">GTEST_TEST(SomeTest,<sp/>DoesThis)<sp/>{<sp/>...<sp/>}</highlight></codeline>
</programlisting> instead of <programlisting><codeline><highlight class="normal">TEST(SomeTest,<sp/>DoesThis)<sp/>{<sp/>...<sp/>}</highlight></codeline>
</programlisting> in order to define a test.</para><para>Currently, the following <computeroutput>TEST</computeroutput>, <computeroutput>FAIL</computeroutput>, <computeroutput>SUCCEED</computeroutput>, and the basic comparison assertion macros can have alternative names. You can see the full list of covered macros <ulink url="http://www.google.com/codesearch?q=if+!GTEST_DONT_DEFINE_\w%2B+package:http://googletest\.googlecode\.com+file:/include/gtest/gtest.h">here</ulink>. More information can be found in the &quot;Avoiding Macro Name Clashes&quot; section of the README file.</para><para><heading level="2">Is it OK if I have two separate <computeroutput>TEST(Foo, Bar)</computeroutput> test methods defined in different namespaces?</heading>
</para><para>Yes.</para><para>The rule is <bold>all test methods in the same test case must use the same fixture class</bold>. This means that the following is <bold>allowed</bold> because both tests use the same fixture class (<computeroutput><ref refid="classtesting_1_1_test" kindref="compound">testing::Test</ref></computeroutput>).</para><para><programlisting><codeline><highlight class="normal">namespace<sp/>foo<sp/>{</highlight></codeline>
<codeline><highlight class="normal">TEST(CoolTest,<sp/>DoSomething)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>SUCCEED();</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
<codeline><highlight class="normal">}<sp/><sp/>//<sp/>namespace<sp/>foo</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">namespace<sp/>bar<sp/>{</highlight></codeline>
<codeline><highlight class="normal">TEST(CoolTest,<sp/>DoSomething)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>SUCCEED();</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
<codeline><highlight class="normal">}<sp/><sp/>//<sp/>namespace<sp/>foo</highlight></codeline>
</programlisting></para><para>However, the following code is <bold>not allowed</bold> and will produce a runtime error from Google Test because the test methods are using different test fixture classes with the same test case name.</para><para><programlisting><codeline><highlight class="normal">namespace<sp/>foo<sp/>{</highlight></codeline>
<codeline><highlight class="normal">class<sp/>CoolTest<sp/>:<sp/>public<sp/>::testing::Test<sp/>{};<sp/><sp/>//<sp/>Fixture<sp/>foo::CoolTest</highlight></codeline>
<codeline><highlight class="normal">TEST_F(CoolTest,<sp/>DoSomething)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>SUCCEED();</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
<codeline><highlight class="normal">}<sp/><sp/>//<sp/>namespace<sp/>foo</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">namespace<sp/>bar<sp/>{</highlight></codeline>
<codeline><highlight class="normal">class<sp/>CoolTest<sp/>:<sp/>public<sp/>::testing::Test<sp/>{};<sp/><sp/>//<sp/>Fixture:<sp/>bar::CoolTest</highlight></codeline>
<codeline><highlight class="normal">TEST_F(CoolTest,<sp/>DoSomething)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>SUCCEED();</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
<codeline><highlight class="normal">}<sp/><sp/>//<sp/>namespace<sp/>foo</highlight></codeline>
</programlisting></para><para><heading level="2">How do I build Google Testing Framework with Xcode 4?</heading>
</para><para>If you try to build Google Test&apos;s Xcode project with Xcode 4.0 or later, you may encounter an error message that looks like &quot;Missing SDK in target gtest\_framework: /Developer/SDKs/MacOSX10.4u.sdk&quot;. That means that Xcode does not support the SDK the project is targeting. See the Xcode section in the ../README.md &quot;README&quot; file on how to resolve this.</para><para><heading level="2">My question is not covered in your FAQ!</heading>
</para><para>If you cannot find the answer to your question in this FAQ, there are some other resources you can use:</para><para><orderedlist>
<listitem><para>read other <ulink url="../docs">wiki pages</ulink>,</para></listitem></orderedlist>
<orderedlist>
<listitem><para>search the mailing list <ulink url="https://groups.google.com/forum/#!forum/googletestframework">archive</ulink>,</para></listitem></orderedlist>
<orderedlist>
<listitem><para>ask it on <ulink url="mailto:googletestframework@googlegroups.com">googletestframework@googlegroups.com</ulink> and someone will answer it (to prevent spam, we require you to join the <ulink url="http://groups.google.com/group/googletestframework">discussion group</ulink> before you can post.).</para></listitem></orderedlist>
</para><para>Please note that creating an issue in the <ulink url="https://github.com/google/googletest/issues">issue tracker</ulink> is <emphasis>not</emphasis> a good way to get your answer, as it is monitored infrequently by a very small number of people.</para><para>When asking a question, it&apos;s helpful to provide as much of the following information as possible (people cannot help you if there&apos;s not enough information in your question):</para><para><itemizedlist>
<listitem><para>the version (or the commit hash if you check out from Git directly) of Google Test you use (Google Test is under active development, so it&apos;s possible that your problem has been solved in a later version),</para></listitem><listitem><para>your operating system,</para></listitem><listitem><para>the name and version of your compiler,</para></listitem><listitem><para>the complete command line flags you give to your compiler,</para></listitem><listitem><para>the complete compiler error messages (if the question is about compilation),</para></listitem><listitem><para>the <emphasis>actual</emphasis> code (ideally, a minimal but complete program) that has the problem you encounter. </para></listitem></itemizedlist>
</para>    </detaileddescription>
  </compounddef>
</doxygen>
